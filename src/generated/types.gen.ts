// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://auth.local.studio.design' | 'https://auth.stg.studio.design' | 'https://auth.studio.design' | (string & {});
};

/**
 * ヘルスチェック応答で返されるステータス情報。
 */
export type HealthStatus = {
    /**
     * サービスが稼働している場合は常に `ok` を返します。
     */
    status: 'ok';
    /**
     * ヘルスチェックを実行した日時。
     */
    checkedAt: string;
};

/**
 * RFC 9457 Problem Details for HTTP APIs を表現する共通レスポンススキーマ。
 *
 * RFC 上はすべてのメンバーが任意ですが、本サービスはコアメンバー
 * (`type` / `title` / `status` / `detail` / `instance`) と共通の拡張メンバー
 * (`trace_id` / `correlation_id` / `service_tag` / `occurred_at` / `log_reference`)
 * を **常に** 返却するため、いずれも `required` として宣言しています。
 * `correlation_id` と `log_reference` は値が `null` になることはありますが、キーは常に存在します。
 *
 * `errors` / `invalid_params` はバリデーション失敗時のみ付与されるため任意です。
 * エンドポイント固有の拡張メンバー（`organization_id` など）は
 * `additionalProperties: true` により追加で返却されることがあります。
 */
export type ProblemDetails = {
    /**
     * 問題タイプを識別する URI 参照 (RFC 3986)。
     * この URI は問題タイプを識別するもので、人間が読める説明へのリンクであることが推奨されます。
     */
    type: string;
    /**
     * 問題タイプの短い、人間が読める要約。
     * ローカライズを除き、同じ問題タイプでは常に同じ値であるべきです。
     */
    title: string;
    /**
     * この問題の HTTP ステータスコード。
     * 情報提供的な性質を持ち、実際の HTTP レスポンスのステータスコードと一致する必要があります。
     */
    status: number;
    /**
     * この問題の特定の発生に関する、人間が読める説明。
     * 問題の詳細を提供し、クライアントが問題を理解・解決するのを助けることに焦点を当てます。
     */
    detail: string;
    /**
     * この問題の特定の発生を識別する URI 参照。
     * 本サービスは `urn:trace:<trace_id>` 形式で返却します。
     */
    instance: string;
    /**
     * W3C Trace Context に準拠した OpenTelemetry の trace ID（32 桁の小文字 16 進数）。
     * サポートへの問い合わせ時にこの値を伝えると、該当リクエストのトレースを特定できます。
     */
    trace_id: string;
    /**
     * クライアントがリクエストヘッダーで指定した相関 ID。
     * 指定がなかった場合は `null` になります（キー自体は常に存在します）。
     */
    correlation_id: string | null;
    /**
     * エラーが発生したサービス／エンドポイントの識別子。
     */
    service_tag: string;
    /**
     * エラーが発生した時刻 (RFC 3339 / ISO 8601)。
     */
    occurred_at: string;
    /**
     * ログ検索用の参照 ID。付与されない場合は `null` になります（キー自体は常に存在します）。
     */
    log_reference: string | null;
    /**
     * バリデーション失敗時のみ付与される、フィールド名をキーとしたエラーメッセージ配列のマップ。
     * 機械可読な用途には `invalid_params` を使用してください。
     */
    errors?: {
        [key: string]: Array<string>;
    };
    /**
     * バリデーション失敗時のみ付与される、失敗したパラメータの一覧。
     */
    invalid_params?: Array<InvalidParam>;
    [key: string]: unknown;
};

export type TokenRequest = ({
    grant_type: 'authorization_code';
} | {
    grant_type: 'refresh_token';
}) & {
    grant_type: GrantType;
    client_id?: ClientId & unknown;
    /**
     * OAuth 2.0 クライアントシークレット。`client_secret_post` 認証方式でのみ使用します。
     *
     * `client_assertion` と同時に送ることはできません（排他）。HTTP Basic 認証でクレデンシャルを送信する場合は本文に含めないでください。
     */
    client_secret?: string;
    /**
     * `client_assertion` が JWT bearer assertion であることを示す識別子 (RFC 7521 §4.2)。
     * `private_key_jwt` 認証方式を使う場合は必ず `client_assertion` と同時に送信してください。
     */
    client_assertion_type?: 'urn:ietf:params:oauth:client-assertion-type:jwt-bearer';
    /**
     * `private_key_jwt` 認証方式 (RFC 7523) におけるクライアントアサーション JWT。
     *
     * - `header.alg`: 認可サーバーが対応する非対称アルゴリズム (`RS256` / `ES256`)
     * - `header.kid`: 認可サーバーにあらかじめ登録された公開鍵 (`client_keys`) の識別子
     * - `iss` / `sub`: ともに `client_id` と一致する必要があります
     * - `aud`: 本エンドポイントの URL または Issuer URL
     * - `exp` / `iat`: 必須。`exp - iat` は認可サーバーの許容ライフタイム以内
     * - `jti`: 必須。リプレイ検知のため短期間の一意性が強制されます
     */
    client_assertion?: string;
    /**
     * `/oauth/authorize` で発行された認可コード。単回使用で、発行後短時間で失効します。
     */
    code?: string;
    redirect_uri?: RedirectUri;
    code_verifier?: PkceVerifierString & unknown;
    /**
     * 事前に発行されたリフレッシュトークン。再発行時には同じスコープまたはそのサブセットを要求できます。
     */
    refresh_token?: string;
    scope?: Scope & unknown;
};

/**
 * RFC 6749 OAuth 2.0 Token Response に準拠したトークン発行レスポンス。
 *
 * アクセストークンを必ず含み、条件に応じてリフレッシュトークンおよび OpenID Connect の ID トークンを含みます。
 */
export type TokenResponse = {
    /**
     * Bearer トークン (JWT)。HTTP Authorization ヘッダーで `Bearer {token}` として利用します。
     */
    access_token: string;
    token_type: TokenType;
    /**
     * アクセストークンの有効期限 (秒)。クライアントはこの値を参考にトークン更新をスケジュールします。
     */
    expires_in: number;
    /**
     * 長寿命トークン。アクセストークンの更新に使用します。セキュアストレージで保管し、漏洩時は即座に失効させてください。
     * 認可リクエストに `offline_access` スコープを含む場合のみ返却されます。
     */
    refresh_token?: string;
    /**
     * OpenID Connect ID Token (JWT)。ユーザー認証結果としてクレームセットを含みます。
     */
    id_token?: string;
    scope?: Scope & unknown;
};

/**
 * RFC 6749 Section 5.2 に準拠した OAuth 2.0 トークンエンドポイントのエラーレスポンス。
 *
 * トークンリクエストが失敗した場合、認可サーバーは HTTP 400 (Bad Request) または
 * 401 (Unauthorized) ステータスコードと共にこのレスポンスを返します。
 */
export type TokenErrorResponse = {
    error: TokenErrorCode;
    /**
     * 人間が読める形式のエラー説明（ASCII 文字のみ）。
     * クライアント開発者がエラーを理解するための追加情報を提供します。
     * エンドユーザーに直接表示することは意図されていません。
     */
    error_description?: string;
    /**
     * エラーに関する追加情報を提供する人間が読める Web ページの URI。
     * クライアント開発者向けのドキュメントへのリンクとして使用されます。
     */
    error_uri?: string;
};

/**
 * OpenID Connect Discovery 1.0 プロバイダーメタデータ。
 * クライアントがエンドポイントやサポート機能を自動検出するために使用します。
 */
export type OpenIdProviderMetadataResponse = {
    /**
     * 発行者識別子 (Issuer Identifier)。
     * HTTPS URL で、クエリやフラグメントを含まない。
     */
    issuer: string;
    /**
     * 認可エンドポイントの URL。
     */
    authorization_endpoint: string;
    /**
     * トークンエンドポイントの URL。
     */
    token_endpoint: string;
    /**
     * UserInfo エンドポイントの URL。
     */
    userinfo_endpoint?: string;
    /**
     * JSON Web Key Set (JWKS) の URL。
     * ID トークンの署名検証に使用する公開鍵を提供します。
     */
    jwks_uri: string;
    /**
     * サポートする OAuth 2.0 スコープ値のリスト。
     */
    scopes_supported?: Array<string>;
    /**
     * サポートする OAuth 2.0 response_type 値のリスト。
     */
    response_types_supported: Array<string>;
    /**
     * サポートする OAuth 2.0 response_mode 値のリスト。
     */
    response_modes_supported?: Array<string>;
    /**
     * サポートする OAuth 2.0 グラントタイプのリスト。
     */
    grant_types_supported?: Array<string>;
    /**
     * サポートする Subject Identifier タイプのリスト。
     */
    subject_types_supported: Array<'public'>;
    /**
     * ID トークンの署名に使用できる JWS アルゴリズムのリスト。
     */
    id_token_signing_alg_values_supported: Array<string>;
    /**
     * トークンエンドポイントでサポートするクライアント認証方式のリスト。
     */
    token_endpoint_auth_methods_supported?: Array<string>;
    /**
     * サポートするクレーム名のリスト。
     */
    claims_supported?: Array<string>;
    /**
     * サポートする PKCE code_challenge_method 値のリスト (RFC 7636)。
     */
    code_challenge_methods_supported?: Array<string>;
    /**
     * サポートする prompt パラメータ値のリスト (OIDC Discovery 1.0 Section 3)。
     */
    prompt_values_supported?: Array<'none' | 'login'>;
    /**
     * トークン取り消しエンドポイントの URL (RFC 7009)。
     */
    revocation_endpoint?: string;
    /**
     * トークンイントロスペクションエンドポイントの URL (RFC 7662)。
     */
    introspection_endpoint?: string;
    /**
     * ログアウトエンドポイントの URL。
     */
    end_session_endpoint?: string;
    [key: string]: unknown;
};

/**
 * JSON Web Key Set (JWKS) レスポンス。
 * ID トークンの署名検証に使用する公開鍵のセットを返します (RFC 7517)。
 */
export type JwksResponse = {
    /**
     * JSON Web Key (JWK) の配列。
     */
    keys: Array<JsonWebKey>;
    [key: string]: unknown;
};

/**
 * UserInfo エンドポイントのレスポンス。
 * アクセストークンに関連付けられたユーザーの情報を返します (OpenID Connect Core 1.0 Section 5.3)。
 *
 * 返却されるクレームは、認可リクエスト時に要求されたスコープに依存します:
 * - `openid`: sub (必須)
 * - `profile`: name
 * - `email`: email, email_verified
 */
export type UserinfoResponse = {
    /**
     * サブジェクト識別子 (Subject Identifier)。
     * 発行者内でユーザーを一意に識別する値。
     */
    sub: string;
    /**
     * ユーザーのフルネーム。
     * `profile` スコープが要求された場合に含まれます。
     */
    name?: string;
    /**
     * ユーザーのメールアドレス。
     * `email` スコープが要求された場合に含まれます。
     */
    email?: string;
    /**
     * メールアドレスが検証済みかどうか。
     * `email` スコープが要求された場合に含まれます。
     */
    email_verified?: boolean;
    [key: string]: unknown;
};

/**
 * RFC 7009 OAuth 2.0 Token Revocation に準拠したトークン無効化リクエスト。
 *
 * クライアントは、不要になったトークン（ログアウト時など）を明示的に無効化することで、
 * トークン漏洩のリスクを軽減できます。
 */
export type RevokeRequest = {
    /**
     * 無効化するトークン文字列。アクセストークンまたはリフレッシュトークンを指定できます。
     */
    token: string;
    token_type_hint?: TokenTypeHint;
};

/**
 * RFC 7009 Section 2.2.1 に準拠した OAuth 2.0 Token Revocation エンドポイントのエラーレスポンス。
 *
 * トークン無効化リクエストが失敗した場合、認可サーバーは HTTP 400 (Bad Request) または
 * 401 (Unauthorized) ステータスコードと共にこのレスポンスを返します。
 *
 * 注意: 無効なトークン（既に失効済み、存在しない等）を無効化しようとしても、
 * エラーにはならず HTTP 200 が返されます（RFC 7009 Section 2.2）。
 */
export type RevokeErrorResponse = {
    error: RevokeErrorCode;
    /**
     * 人間が読める形式のエラー説明（ASCII 文字のみ）。
     * クライアント開発者がエラーを理解するための追加情報を提供します。
     * エンドユーザーに直接表示することは意図されていません。
     */
    error_description?: string;
    /**
     * エラーに関する追加情報を提供する人間が読める Web ページの URI。
     * クライアント開発者向けのドキュメントへのリンクとして使用されます。
     */
    error_uri?: string;
};

/**
 * RFC 7662 OAuth 2.0 Token Introspection に準拠したトークンイントロスペクションリクエスト。
 *
 * リソースサーバーは、受け取ったトークンの有効性とメタ情報を確認するために
 * このリクエストを送信します。
 *
 * クライアント認証は HTTP Basic 認証 (RFC 6749 Section 2.3.1) で行われるため、
 * リクエストボディには含まれません。
 */
export type IntrospectRequest = {
    /**
     * イントロスペクション対象のトークン文字列。アクセストークンまたはリフレッシュトークンを指定できます。
     */
    token: string;
    token_type_hint?: TokenTypeHint;
};

/**
 * End Session エンドポイントへの POST リクエストボディ。
 * すべてのプロパティはオプションです。
 */
export type EndSessionRequest = {
    /**
     * ログアウト対象のユーザーを特定するための ID トークン。
     *
     * OP (OpenID Provider) は、このトークンを使用してログアウト対象の
     * エンドユーザーのセッションを特定します。トークンは暗号化されていても構いません。
     *
     * OP は必ずしもトークンの署名を検証する必要はありませんが、
     * `aud` クレームに自身が含まれていることを確認します。
     */
    id_token_hint?: string;
    /**
     * ログアウト対象のエンドユーザーに関するヒント。
     *
     * OP がエンドユーザーを特定するために使用できる追加情報です。
     * `id_token_hint` が提供されていない場合に、ユーザーを特定するための
     * 補助的な手段として使用されることがあります。
     */
    logout_hint?: string;
    /**
     * ログアウト完了後にユーザーをリダイレクトする URI。
     *
     * この URI は、事前にクライアント登録時に `post_logout_redirect_uris` として
     * 登録されている必要があります。登録されていない URI が指定された場合、
     * リダイレクトは行われません。
     */
    post_logout_redirect_uri?: string;
    client_id?: ClientId;
    /**
     * RP がセッション状態を維持するための不透明な値。
     *
     * `post_logout_redirect_uri` へのリダイレクト時にクエリパラメータとして
     * そのまま返却されます。CSRF 攻撃を防ぐために使用できます。
     */
    state?: string;
    /**
     * エンドユーザーの優先言語およびスクリプトを表す BCP47 言語タグのスペース区切りリスト。
     *
     * OP はこの値を使用して、ログアウト確認画面やエラーメッセージの表示言語を
     * 決定することができます。優先度の高い順に並べます。
     */
    ui_locales?: string;
};

/**
 * RFC 7662 OAuth 2.0 Token Introspection に準拠したトークンイントロスペクションレスポンス。
 *
 * `active` フィールドは常に含まれ、トークンが有効かどうかを示します。
 * トークンが有効な場合（`active: true`）、追加のメタ情報（スコープ、有効期限等）が含まれます。
 * トークンが無効な場合（`active: false`）、通常は `active` フィールドのみが返されます。
 *
 * 認可サーバーは、トークンの種類やリソースサーバーに応じて、
 * 返却する情報を制限する場合があります。
 */
export type IntrospectResponse = {
    /**
     * トークンが現在アクティブ（有効）かどうかを示すブール値。
     *
     * `true` の場合、トークンは有効であり、追加のメタ情報が含まれます。
     * `false` の場合、トークンは無効（期限切れ、失効済み、不正等）です。
     */
    active: boolean;
    /**
     * トークンに関連付けられたスコープのスペース区切りリスト。
     * 認可サーバーは、リソースサーバーに応じて返却するスコープを制限する場合があります。
     */
    scope?: string;
    /**
     * トークンを要求したクライアントの識別子。
     */
    client_id?: string;
    /**
     * トークンに関連付けられたリソースオーナーの人間が読める識別子。
     */
    username?: string;
    token_type?: TokenType;
    /**
     * トークンの有効期限を示す UNIX タイムスタンプ（秒）。
     * RFC 7519 Section 4.1.4 で定義されています。
     */
    exp?: number;
    /**
     * トークンが発行された時刻を示す UNIX タイムスタンプ（秒）。
     * RFC 7519 Section 4.1.6 で定義されています。
     */
    iat?: number;
    /**
     * トークンが有効になる時刻（この時刻より前は無効）を示す UNIX タイムスタンプ（秒）。
     * RFC 7519 Section 4.1.5 で定義されています。
     */
    nbf?: number;
    /**
     * トークンのサブジェクト（主体）を識別する一意の識別子。
     * 通常はリソースオーナー（ユーザー）の識別子です。
     * RFC 7519 Section 4.1.2 で定義されています。
     */
    sub?: string;
    /**
     * トークンの対象となるオーディエンス（受信者）。
     * 単一の文字列または文字列の配列で表されます。
     * RFC 7519 Section 4.1.3 で定義されています。
     */
    aud?: string | Array<string>;
    /**
     * トークンを発行した認可サーバーの識別子（URL）。
     * RFC 7519 Section 4.1.1 で定義されています。
     */
    iss?: string;
    /**
     * トークンの一意識別子（JWT ID）。
     * RFC 7519 Section 4.1.7 で定義されています。
     */
    jti?: string;
    /**
     * トークンに関連付けられたユーザーのメールアドレス。
     * "email" スコープがトークンのスコープに含まれる場合のみ返されます。
     */
    email?: string;
    /**
     * ユーザーのメールアドレスが検証済みかどうかを示すブール値。
     * "email" スコープがトークンのスコープに含まれる場合のみ返されます。
     */
    email_verified?: boolean;
    /**
     * トークンに紐づく組織識別子（UUID形式）。
     * アクセストークンに `org_id` クレームが含まれている場合に返却されます。
     * 組織コンテキストを持たないトークンの場合は `null` または不在となります。
     */
    org_id?: string | null;
    /**
     * トークンに紐づく組織が SSO ログインを強制しているかどうかを示すブール値。
     * アクセストークンに `org_id` クレームが含まれ、その組織が解決できた場合のみ返却されます。
     * 値は introspection 時点の組織設定を反映します（トークン発行時点ではありません）。
     * `org_id` を持たないトークンや組織が解決できない場合は不在となります。
     */
    sso_enforced?: boolean;
    [key: string]: unknown;
};

/**
 * RFC 7662 Section 2.3 に準拠した OAuth 2.0 Token Introspection エンドポイントのエラーレスポンス。
 *
 * トークンイントロスペクションリクエストが失敗した場合、認可サーバーは
 * HTTP 400 (Bad Request) または 401 (Unauthorized) ステータスコードと共にこのレスポンスを返します。
 *
 * 注意: 無効なトークン（期限切れ、失効済み、存在しない等）を検証しようとしても、
 * エラーにはならず HTTP 200 で `active: false` が返されます（RFC 7662 Section 2.2）。
 */
export type IntrospectErrorResponse = {
    error: IntrospectErrorCode;
    /**
     * 人間が読める形式のエラー説明（ASCII 文字のみ）。
     * クライアント開発者がエラーを理解するための追加情報を提供します。
     * エンドユーザーに直接表示することは意図されていません。
     */
    error_description?: string;
    /**
     * エラーに関する追加情報を提供する人間が読める Web ページの URI。
     * クライアント開発者向けのドキュメントへのリンクとして使用されます。
     */
    error_uri?: string;
};

/**
 * OAuth 2.0 クライアント情報。
 * 認可サーバーに登録されたクライアントアプリケーションを表します。
 */
export type Client = {
    /**
     * クライアント識別子（一意）。
     */
    client_id: string;
    /**
     * クライアントアプリケーションの表示名。
     */
    client_name: string;
    /**
     * クライアントの説明。
     */
    description?: string | null;
    /**
     * クライアントタイプ。
     *
     * - `confidential`: 機密クライアント（サーバーサイドアプリ）。クライアントシークレットを安全に保持可能。
     */
    client_type: 'confidential';
    /**
     * 許可されたリダイレクト URI のリスト。
     */
    redirect_uris: Array<string>;
    /**
     * RP-initiated logout（OIDC）でログアウト後に戻す先として登録された URI のリスト。
     * 登録が無い場合は空配列を返します。
     */
    post_logout_redirect_uris: Array<string>;
    /**
     * このクライアントに許可されたスコープのリスト。
     */
    allowed_scopes: Array<'openid' | 'profile' | 'email' | 'offline_access'>;
    /**
     * このクライアントに許可されたグラントタイプのリスト。
     */
    grant_types_supported: Array<'authorization_code' | 'refresh_token'>;
    /**
     * トークンエンドポイントでのクライアント認証方式。
     *
     * - `client_secret_basic`: HTTP Basic 認証
     * - `client_secret_post`: リクエストボディでの送信
     * - `private_key_jwt`: JWT による認証
     */
    token_endpoint_auth_method: 'client_secret_basic' | 'client_secret_post' | 'private_key_jwt';
    /**
     * クライアントが有効かどうか。無効なクライアントは認可リクエストを受け付けません。
     */
    is_active: boolean;
    /**
     * クライアント登録日時（ISO 8601 形式）。
     */
    created_at: string;
    /**
     * クライアント最終更新日時（ISO 8601 形式）。
     */
    updated_at: string;
};

/**
 * 組織情報。
 * システムに登録された組織を表します。
 */
export type Organization = {
    /**
     * 組織識別子（UUID形式、一意）。
     */
    organization_id: string;
    /**
     * 組織の識別名（一意）。システム内部での識別に使用されます。
     *
     * - 小文字英数字とハイフンのみ使用可能
     * - 先頭・末尾にハイフンは使用不可
     */
    name: string;
    /**
     * 組織の表示名。UI上での表示に使用されます。
     */
    display_name: string | null;
    /**
     * 組織が有効かどうか。無効な組織は操作を受け付けません。
     */
    is_active: boolean;
    /**
     * SSOログインを強制するかどうか。
     * `true` の場合、この組織のドメインに属するユーザーはSSO認証のみが許可されます。
     */
    is_sso_enforced: boolean;
    /**
     * 組織登録日時（ISO 8601 形式）。
     */
    created_at: string;
    /**
     * 組織最終更新日時（ISO 8601 形式）。
     */
    updated_at: string;
};

/**
 * 組織メンバー向けエンドポイントが返す組織情報。
 * 共通の `Organization` フィールドに加え、ドメイン認証 / SSO connection の派生ステータスを含みます。
 */
export type MyOrganization = Organization & {
    /**
     * 組織のドメイン認証の集約ステータス。
     * - `verified`: 認証済みドメインが 1 つ以上存在する
     * - `pending`: 申請中ドメインのみ存在する
     * - `none`: 申請されたドメインが存在しない、または外部プロバイダ未連携
     *
     * 外部プロバイダ (WorkOS) API 呼び出しに失敗した場合は `none` を返却します。
     */
    domain_verification_status: 'verified' | 'pending' | 'none';
    /**
     * 組織の SSO connection の集約ステータス。
     * - `active`: 有効化された connection が 1 つ以上存在する
     * - `validating`: 検証中の connection のみ存在する
     * - `inactive`: 登録済みだが無効化された connection のみ存在する
     * - `none`: connection が登録されていない、または外部プロバイダ未連携
     *
     * `is_sso_enforced` が SSO 強制 (ON/OFF) を表すのに対し、本フィールドは
     * 設定済み connection の有無を表す独立した派生ステータスです。
     * 外部プロバイダ (WorkOS) API 呼び出しに失敗した場合は `none` を返却します。
     */
    sso_connection_status: 'active' | 'validating' | 'inactive' | 'none';
};

/**
 * 組織メンバー情報。
 */
export type OrganizationMember = {
    /**
     * メンバー識別子（UUID形式）。
     */
    id: string;
    user: OrganizationMemberUser;
    role: OrganizationRole;
    /**
     * メンバーのアバター画像 URL。アバター未設定時は `null`。
     * ユーザーレコードの `picture_url` を参照します。
     */
    avatar_src?: string | null;
    /**
     * 組織への最終アクセス日時（ISO 8601 形式）。
     * 組織コンテキストでまだアクセスしていないメンバーは `null`。
     */
    last_access?: string | null;
    domain_type?: OrganizationMemberDomainType;
    /**
     * 組織への参加日時（ISO 8601 形式）。
     */
    joined_at: string;
};

/**
 * 組織メンバーのユーザー情報。
 */
export type OrganizationMemberUser = {
    /**
     * ユーザー識別子（UUID形式）。
     */
    id: string;
    /**
     * ユーザーのメールアドレス。
     */
    email: string;
    /**
     * ユーザーの表示名（姓名を結合したロケール依存の形式）。未設定時は `null`。
     */
    name: string | null;
    /**
     * ユーザーの名（given name）。姓名を分けて表示するクライアント向け。未設定時は `null`。
     */
    given_name: string | null;
    /**
     * ユーザーの姓（family name）。姓名を分けて表示するクライアント向け。未設定時は `null`。
     */
    family_name: string | null;
};

/**
 * メンバーのメールドメインが、組織に登録された verified ドメイン（`organization_email_domains`）と一致するかどうかを示す分類。
 * - `corporate`: メンバーのメールドメインが組織の verified ドメインと一致する。
 * - `personal`: 一致しない（フリーメールや個人利用と想定されるドメインを含む）。
 *
 * この分類はメンバーの email から動的に算出されるため、組織のドメイン登録状況に応じて変化します。
 */
export const OrganizationMemberDomainType = { CORPORATE: 'corporate', PERSONAL: 'personal' } as const;

/**
 * メンバーのメールドメインが、組織に登録された verified ドメイン（`organization_email_domains`）と一致するかどうかを示す分類。
 * - `corporate`: メンバーのメールドメインが組織の verified ドメインと一致する。
 * - `personal`: 一致しない（フリーメールや個人利用と想定されるドメインを含む）。
 *
 * この分類はメンバーの email から動的に算出されるため、組織のドメイン登録状況に応じて変化します。
 */
export type OrganizationMemberDomainType = typeof OrganizationMemberDomainType[keyof typeof OrganizationMemberDomainType];

/**
 * ロール別のメンバー数集計（フィルタ適用前の総件数）。
 * `include_counts=true` の場合のみレスポンスに含まれます。
 */
export type MemberRoleCounts = {
    /**
     * Owner ロールのメンバー数。
     */
    owner: number;
    /**
     * Admin ロールのメンバー数。
     */
    admin: number;
    /**
     * Security Admin ロールのメンバー数。
     */
    security_admin: number;
    /**
     * 一般 Member ロールのメンバー数。
     */
    member: number;
};

/**
 * メールドメイン分類別のメンバー数集計（フィルタ適用前の総件数）。
 * `include_counts=true` の場合のみレスポンスに含まれます。
 */
export type MemberDomainTypeCounts = {
    /**
     * 組織の verified ドメインに一致するメールを持つメンバー数。
     */
    corporate: number;
    /**
     * 組織の verified ドメインに一致しないメールを持つメンバー数。
     */
    personal: number;
};

/**
 * ページネーション情報。
 */
export type PaginationMeta = {
    /**
     * 現在のページ番号（1始まり）。
     */
    current_page: number;
    /**
     * 1ページあたりの件数。
     */
    per_page: number;
    /**
     * 総件数。
     */
    total: number;
    /**
     * 最終ページ番号。
     */
    last_page: number;
};

/**
 * クライアント登録リクエスト。
 */
export type AdminClientCreateRequest = {
    /**
     * クライアントアプリケーションの表示名。
     */
    client_name: string;
    /**
     * クライアントの説明（オプション）。
     */
    description?: string;
    /**
     * クライアントタイプ。
     *
     * - `confidential`: 機密クライアント（サーバーサイドアプリ）
     */
    client_type: 'confidential';
    /**
     * 許可するリダイレクト URI のリスト。
     */
    redirect_uris: Array<string>;
    /**
     * RP-initiated logout（OIDC）でログアウト後に戻す URI のリスト（オプション）。
     * ここに登録された URI のみ end-session の `post_logout_redirect_uri` として利用できます。
     */
    post_logout_redirect_uris?: Array<string>;
    /**
     * このクライアントに許可するスコープのリスト。
     */
    allowed_scopes: Array<'openid' | 'profile' | 'email' | 'offline_access'>;
    /**
     * このクライアントに許可するグラントタイプのリスト。
     */
    grant_types_supported: Array<'authorization_code' | 'refresh_token'>;
    /**
     * トークンエンドポイントでのクライアント認証方式。
     *
     * - `client_secret_basic`: HTTP Basic 認証（デフォルト）
     * - `client_secret_post`: リクエストボディでの送信
     * - `private_key_jwt`: JWT による認証
     */
    token_endpoint_auth_method?: 'client_secret_basic' | 'client_secret_post' | 'private_key_jwt';
};

/**
 * クライアント登録成功レスポンス。
 *
 * **重要**: `client_secret` はこのレスポンスでのみ返却されます。
 * 後から再取得することはできないため、安全な場所に保存してください。
 */
export type AdminClientCreatedResponse = {
    /**
     * 発行されたクライアント識別子。
     */
    client_id: string;
    /**
     * 発行されたクライアントシークレット。
     *
     * **重要**: この値は一度しか表示されません。安全な場所に保存してください。
     */
    client_secret: string;
    /**
     * クライアントアプリケーションの表示名。
     */
    client_name: string;
    /**
     * クライアントの説明。
     */
    description?: string | null;
    /**
     * クライアントタイプ。
     */
    client_type: 'confidential';
    /**
     * 許可されたリダイレクト URI のリスト。
     */
    redirect_uris: Array<string>;
    /**
     * RP-initiated logout（OIDC）でログアウト後に戻す先として登録された URI のリスト。
     */
    post_logout_redirect_uris: Array<string>;
    /**
     * このクライアントに許可されたスコープのリスト。
     */
    allowed_scopes: Array<string>;
    /**
     * このクライアントに許可されたグラントタイプのリスト。
     */
    grant_types_supported: Array<string>;
    /**
     * トークンエンドポイントでのクライアント認証方式。
     */
    token_endpoint_auth_method: string;
    /**
     * クライアントが有効かどうか。
     */
    is_active: boolean;
    /**
     * クライアント登録日時（ISO 8601 形式）。
     */
    created_at: string;
    /**
     * クライアント最終更新日時（ISO 8601 形式）。
     */
    updated_at: string;
};

/**
 * クライアント一覧レスポンス（ページネーション付き）。
 */
export type AdminClientListResponse = {
    /**
     * クライアントの配列。
     */
    data: Array<Client>;
    meta: PaginationMeta;
};

/**
 * 組織一覧レスポンス（ページネーション付き）。
 */
export type AdminOrganizationListResponse = {
    /**
     * 組織の配列。
     */
    data: Array<Organization>;
    meta: PaginationMeta;
};

/**
 * クライアント更新リクエスト。
 * 指定されたフィールドのみが更新されます。
 */
export type AdminClientUpdateRequest = {
    /**
     * クライアントアプリケーションの表示名。
     */
    client_name?: string;
    /**
     * クライアントの説明。`null` を指定すると説明を削除します。
     */
    description?: string | null;
    /**
     * 許可するリダイレクト URI のリスト。
     */
    redirect_uris?: Array<string>;
    /**
     * RP-initiated logout（OIDC）でログアウト後に戻す URI のリスト。
     * 指定した場合は既存の登録をこの内容で置き換えます。空配列を指定するとすべて削除します。
     */
    post_logout_redirect_uris?: Array<string>;
    /**
     * このクライアントに許可するスコープのリスト。
     */
    allowed_scopes?: Array<'openid' | 'profile' | 'email' | 'offline_access'>;
    /**
     * このクライアントに許可するグラントタイプのリスト。
     */
    grant_types_supported?: Array<'authorization_code' | 'refresh_token'>;
    /**
     * トークンエンドポイントでのクライアント認証方式。
     *
     * - `client_secret_basic`: HTTP Basic 認証
     * - `client_secret_post`: リクエストボディでの送信
     * - `private_key_jwt`: JWT による認証
     */
    token_endpoint_auth_method?: 'client_secret_basic' | 'client_secret_post' | 'private_key_jwt';
    /**
     * クライアントの有効/無効を切り替えます。
     */
    is_active?: boolean;
};

/**
 * 組織登録リクエスト。
 */
export type AdminOrganizationCreateRequest = {
    /**
     * 組織の識別名。システム内部での識別に使用されます。
     *
     * - 小文字英数字とハイフンのみ使用可能
     * - 先頭・末尾にハイフンは使用不可
     * - システム内で一意である必要があります
     */
    name: string;
    /**
     * 組織の表示名（オプション）。UI上での表示に使用されます。`null` を指定するか省略すると表示名なしになります。
     */
    display_name?: string | null;
};

/**
 * 組織登録成功レスポンス。
 */
export type AdminOrganizationCreatedResponse = {
    /**
     * 発行された組織識別子（UUID形式）。
     */
    organization_id: string;
    /**
     * 組織の識別名。
     */
    name: string;
    /**
     * 組織の表示名。
     */
    display_name?: string | null;
    /**
     * 組織が有効かどうか。
     */
    is_active: boolean;
    /**
     * 組織登録日時（ISO 8601 形式）。
     */
    created_at: string;
    /**
     * 組織最終更新日時（ISO 8601 形式）。
     */
    updated_at: string;
};

/**
 * 組織更新リクエスト。
 * 指定されたフィールドのみが更新されます。
 */
export type AdminOrganizationUpdateRequest = {
    /**
     * 組織の識別名。システム内部での識別に使用されます。
     *
     * - 小文字英数字とハイフンのみ使用可能
     * - 先頭・末尾にハイフンは使用不可
     * - システム内で一意である必要があります
     */
    name?: string;
    /**
     * 組織の表示名。`null` を指定すると表示名を削除します。
     */
    display_name?: string | null;
    /**
     * 組織の有効/無効を切り替えます。
     * `false` を指定すると組織を無効化（論理削除）し、
     * `true` を指定すると再有効化します。
     */
    is_active?: boolean;
    /**
     * SSOログインを強制するかどうかを切り替えます。
     * `true` を指定すると、この組織のドメインに属するユーザーはSSO認証のみが許可されます。
     */
    is_sso_enforced?: boolean;
};

/**
 * 組織メンバー一覧レスポンス（ページネーション付き）。
 * `include_counts=true` クエリ指定時のみ `role_counts` と `domain_counts` を含みます（フィルタ適用前の総件数）。
 */
export type AdminOrganizationMemberListResponse = {
    /**
     * 組織メンバーの配列。
     */
    data: Array<OrganizationMember>;
    meta: PaginationMeta;
    role_counts?: MemberRoleCounts;
    domain_counts?: MemberDomainTypeCounts;
};

/**
 * 組織メンバー一覧レスポンス（ページネーション付き）。
 * `include_counts=true` クエリ指定時のみ `role_counts` と `domain_counts` を含みます（フィルタ適用前の総件数）。
 */
export type OrganizationMemberListResponse = {
    /**
     * 組織メンバーの配列。
     */
    data: Array<OrganizationMember>;
    meta: PaginationMeta;
    role_counts?: MemberRoleCounts;
    domain_counts?: MemberDomainTypeCounts;
};

/**
 * メンバーロール変更リクエスト。
 */
export type AdminOrganizationMemberUpdateRequest = {
    role: OrganizationRole;
};

/**
 * 組織内でのロール。
 * - `owner`: 組織のオーナー。すべての権限を持つ。
 * - `admin`: 管理者。メンバーの招待・削除および請求管理が可能。ロール変更やセキュリティ管理は不可。
 * - `security_admin`: セキュリティ管理者。セキュリティ設定の管理が可能。メンバー管理や請求管理は不可。
 * - `member`: 一般メンバー。管理権限なし。
 */
export const OrganizationRole = {
    OWNER: 'owner',
    ADMIN: 'admin',
    SECURITY_ADMIN: 'security_admin',
    MEMBER: 'member'
} as const;

/**
 * 組織内でのロール。
 * - `owner`: 組織のオーナー。すべての権限を持つ。
 * - `admin`: 管理者。メンバーの招待・削除および請求管理が可能。ロール変更やセキュリティ管理は不可。
 * - `security_admin`: セキュリティ管理者。セキュリティ設定の管理が可能。メンバー管理や請求管理は不可。
 * - `member`: 一般メンバー。管理権限なし。
 */
export type OrganizationRole = typeof OrganizationRole[keyof typeof OrganizationRole];

/**
 * 組織への招待情報。
 * メンバーを組織に招待する際に作成されます。
 */
export type OrganizationInvitation = {
    /**
     * 招待識別子（UUID形式、一意）。
     */
    id: string;
    /**
     * 招待先のメールアドレス。
     */
    email: string;
    role: OrganizationRole;
    /**
     * 招待のステータス。
     * - `pending`: 招待が送信され、承認待ち。
     * - `accepted`: 招待が承認された。
     * - `expired`: 招待の有効期限が切れた。
     * - `revoked`: 招待が取り消された。
     */
    status: 'pending' | 'accepted' | 'expired' | 'revoked';
    /**
     * 招待の有効期限（ISO 8601 形式）。
     */
    expires_at: string;
    /**
     * 招待作成日時（ISO 8601 形式）。
     */
    created_at: string;
};

/**
 * 組織へのメンバー招待リクエスト。
 */
export type AdminOrganizationInvitationCreateRequest = {
    /**
     * 招待先のメールアドレス。
     */
    email: string;
    role: OrganizationRole;
};

/**
 * 組織へのメンバー招待成功レスポンス。
 */
export type AdminOrganizationInvitationCreatedResponse = {
    /**
     * 発行された招待識別子（UUID形式）。
     */
    id: string;
    /**
     * 招待先のメールアドレス。
     */
    email: string;
    role: OrganizationRole;
    /**
     * 招待のステータス。新規作成時は常に `pending`。
     */
    status: 'pending';
    /**
     * 招待の有効期限（ISO 8601 形式）。
     */
    expires_at: string;
    /**
     * 招待の作成日時（ISO 8601 形式）。
     */
    created_at: string;
};

/**
 * WorkOS Admin Portal セッション作成リクエスト。
 * Admin Portal は組織のIT管理者がSSO接続の設定、ドメイン認証、監査ログの閲覧等をセルフサービスで行うための画面です。
 */
export type AdminPortalSessionCreateRequest = {
    /**
     * ポータルセッションの用途。
     * - `sso`: SSO接続の設定
     * - `domain_verification`: ドメイン認証の設定
     * - `audit_log`: 監査ログの閲覧
     */
    intent: 'sso' | 'domain_verification' | 'audit_log';
};

/**
 * WorkOS Admin Portal セッション作成成功レスポンス。
 * 生成されたポータル URL は5分間有効です。
 */
export type AdminPortalSessionCreatedResponse = {
    /**
     * WorkOS Admin Portal の URL。
     * IT管理者がこの URL にアクセスすることで、SSO接続の設定を行えます。
     * この URL は発行から5分間有効です。
     */
    admin_portal_url: string;
};

/**
 * 組織Adminによるメンバー招待リクエスト。
 */
export type OrganizationInvitationCreateRequest = {
    /**
     * 招待先のメールアドレス。
     */
    email: string;
    role: OrganizationRole;
};

/**
 * 組織Adminによるメンバー招待成功レスポンス。
 */
export type OrganizationInvitationCreatedResponse = {
    /**
     * 発行された招待識別子（UUID形式）。
     */
    id: string;
    /**
     * 招待先のメールアドレス。
     */
    email: string;
    role: OrganizationRole;
    /**
     * 招待のステータス。新規作成時は常に `pending`。
     */
    status: 'pending';
    /**
     * 招待の有効期限（ISO 8601 形式）。
     */
    expires_at: string;
    /**
     * 招待の作成日時（ISO 8601 形式）。
     */
    created_at: string;
};

/**
 * メンバーロール変更リクエスト（組織メンバー向け）。
 */
export type OrganizationMemberUpdateRequest = {
    role: OrganizationRole;
};

/**
 * SSO 強制判定レスポンス
 */
export type CheckSsoEnforcementResponse = {
    /**
     * SSO 強制が適用されているかどうか
     */
    enforced: boolean;
    /**
     * SSO 強制を適用している組織の ID。強制されていない場合は null
     */
    org_id: string | null;
};

/**
 * SSO 強制判定エンドポイントのエラーレスポンス
 */
export type SsoEnforcementErrorResponse = {
    /**
     * エラーコード
     */
    error: string;
    /**
     * エラーの説明
     */
    error_description: string;
};

/**
 * サービス情報のレスポンススキーマ。サービス名と OpenID Connect Discovery エンドポイントへのリンクを含みます。
 */
export type ServiceInfoResponse = {
    /**
     * サービス名
     */
    name: string;
    /**
     * OpenID Connect Discovery エンドポイントの URL
     */
    documentation: string;
};

/**
 * OAuth 2.0 認可リクエストのレスポンスタイプ (RFC 6749 Section 3.1.1)。本実装は Authorization Code フロー のみをサポートします。
 *
 * - `code`: Authorization Code フロー。認可サーバーは `redirect_uri` に認可コードを付与してリダイレクトします。
 */
export const ResponseType = { CODE: 'code' } as const;

/**
 * OAuth 2.0 認可リクエストのレスポンスタイプ (RFC 6749 Section 3.1.1)。本実装は Authorization Code フロー のみをサポートします。
 *
 * - `code`: Authorization Code フロー。認可サーバーは `redirect_uri` に認可コードを付与してリダイレクトします。
 */
export type ResponseType2 = typeof ResponseType[keyof typeof ResponseType];

/**
 * OAuth 2.0 グラントタイプ (RFC 6749)。
 *
 * - `authorization_code`: Authorization Code グラント (RFC 6749 Section 4.1)。`/oauth/authorize` で発行された認可コードをアクセストークンに交換します。
 * - `refresh_token`: Refresh Token グラント (RFC 6749 Section 6)。有効期限切れのアクセストークンを再発行します。
 */
export const GrantType = { /**
     * AUTHORIZATION_CODE
     */
    AUTHORIZATION_CODE: 'authorization_code', /**
     * REFRESH_TOKEN
     */
    REFRESH_TOKEN: 'refresh_token' } as const;

/**
 * OAuth 2.0 グラントタイプ (RFC 6749)。
 *
 * - `authorization_code`: Authorization Code グラント (RFC 6749 Section 4.1)。`/oauth/authorize` で発行された認可コードをアクセストークンに交換します。
 * - `refresh_token`: Refresh Token グラント (RFC 6749 Section 6)。有効期限切れのアクセストークンを再発行します。
 */
export type GrantType = typeof GrantType[keyof typeof GrantType];

/**
 * OAuth 2.0 トークンタイプ (RFC 6749 Section 7.1 / RFC 6750)。本実装は Bearer トークンのみをサポートします。
 *
 * - `Bearer`: HTTP `Authorization: Bearer {token}` ヘッダーで提示する Bearer トークン。
 */
export const TokenType = { BEARER: 'Bearer' } as const;

/**
 * OAuth 2.0 トークンタイプ (RFC 6749 Section 7.1 / RFC 6750)。本実装は Bearer トークンのみをサポートします。
 *
 * - `Bearer`: HTTP `Authorization: Bearer {token}` ヘッダーで提示する Bearer トークン。
 */
export type TokenType = typeof TokenType[keyof typeof TokenType];

/**
 * クライアントが対象にしようとしているトークンの種類を示すヒント。
 * RFC 7009 (Revocation) および RFC 7662 (Introspection) で定義されます。
 *
 * 認可サーバーはこのヒントを参考にトークン検索を最適化できますが、
 * ヒントと異なる種類のトークンであっても処理を試みます。
 *
 * - `access_token`: 短寿命のリソースアクセス用 Bearer トークン
 * - `refresh_token`: 長寿命のアクセストークン再発行用トークン
 */
export const TokenTypeHint = { ACCESS_TOKEN: 'access_token', REFRESH_TOKEN: 'refresh_token' } as const;

/**
 * クライアントが対象にしようとしているトークンの種類を示すヒント。
 * RFC 7009 (Revocation) および RFC 7662 (Introspection) で定義されます。
 *
 * 認可サーバーはこのヒントを参考にトークン検索を最適化できますが、
 * ヒントと異なる種類のトークンであっても処理を試みます。
 *
 * - `access_token`: 短寿命のリソースアクセス用 Bearer トークン
 * - `refresh_token`: 長寿命のアクセストークン再発行用トークン
 */
export type TokenTypeHint = typeof TokenTypeHint[keyof typeof TokenTypeHint];

/**
 * RFC 6749 Section 5.2 で定義される OAuth 2.0 Token エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 * - `invalid_grant`: 認可コードまたはリフレッシュトークンが無効、期限切れ、失効、または `redirect_uri` 不一致
 * - `unauthorized_client`: 認証されたクライアントがこのグラントタイプを使用する権限がない
 * - `unsupported_grant_type`: 認可サーバーがこのグラントタイプをサポートしていない
 * - `invalid_scope`: 要求されたスコープが無効、未知、不正、または元の認可範囲を超過
 */
export const TokenErrorCode = {
    INVALID_REQUEST: 'invalid_request',
    INVALID_CLIENT: 'invalid_client',
    INVALID_GRANT: 'invalid_grant',
    UNAUTHORIZED_CLIENT: 'unauthorized_client',
    UNSUPPORTED_GRANT_TYPE: 'unsupported_grant_type',
    INVALID_SCOPE: 'invalid_scope'
} as const;

/**
 * RFC 6749 Section 5.2 で定義される OAuth 2.0 Token エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 * - `invalid_grant`: 認可コードまたはリフレッシュトークンが無効、期限切れ、失効、または `redirect_uri` 不一致
 * - `unauthorized_client`: 認証されたクライアントがこのグラントタイプを使用する権限がない
 * - `unsupported_grant_type`: 認可サーバーがこのグラントタイプをサポートしていない
 * - `invalid_scope`: 要求されたスコープが無効、未知、不正、または元の認可範囲を超過
 */
export type TokenErrorCode = typeof TokenErrorCode[keyof typeof TokenErrorCode];

/**
 * RFC 7009 Section 2.2.1 および RFC 6749 Section 5.2 で定義される
 * `/oauth/revoke` エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 * - `unsupported_token_type`: 認可サーバーが提示されたトークンタイプの無効化をサポートしていない
 */
export const RevokeErrorCode = {
    INVALID_REQUEST: 'invalid_request',
    INVALID_CLIENT: 'invalid_client',
    UNSUPPORTED_TOKEN_TYPE: 'unsupported_token_type'
} as const;

/**
 * RFC 7009 Section 2.2.1 および RFC 6749 Section 5.2 で定義される
 * `/oauth/revoke` エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 * - `unsupported_token_type`: 認可サーバーが提示されたトークンタイプの無効化をサポートしていない
 */
export type RevokeErrorCode = typeof RevokeErrorCode[keyof typeof RevokeErrorCode];

/**
 * RFC 7662 Section 2.3 および RFC 6749 Section 5.2 で定義される
 * `/oauth/introspect` エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 */
export const IntrospectErrorCode = { INVALID_REQUEST: 'invalid_request', INVALID_CLIENT: 'invalid_client' } as const;

/**
 * RFC 7662 Section 2.3 および RFC 6749 Section 5.2 で定義される
 * `/oauth/introspect` エンドポイントのエラーコード。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、重複パラメータ、または不正な形式
 * - `invalid_client`: クライアント認証失敗（未知のクライアント、認証情報なし、サポートされていない認証方式）
 */
export type IntrospectErrorCode = typeof IntrospectErrorCode[keyof typeof IntrospectErrorCode];

/**
 * `/oauth/authorize` が 302 リダイレクトの `?error=` クエリパラメータに設定するエラーコード
 * (OIDC Core 1.0 Section 3.1.2.6)。
 *
 * RFC 6749 Section 4.1.2.1 が定義する他のエラー (`invalid_request` /
 * `unsupported_response_type` など) は本実装では 422 Problem Details として
 * `application/problem+json` 形式で返され、`OauthAuthorizeErrorCode` を参照してください。
 *
 * どちらも `prompt=none` が指定されたときにのみ返されます。`prompt=none` でなければ、
 * 対話が必要な状況では認可サーバがそのための画面を表示します。
 *
 * - `login_required`: 対話的な**認証**が必要だが `prompt=none` のため実行できない
 * (OIDC Core Section 3.1.2.6)。セッションが無い場合と、SSO 強制 organization に対して
 * 非 SSO セッションを再利用しようとした場合に返します
 * - `interaction_required`: ユーザーは認証済みだが、続行するには対話が必要
 * (OIDC Core Section 3.1.2.6)。active な organization に 2 件以上所属していて
 * `organization_id` の指定が無く、どの organization にトークンをバインドするか
 * 確定できない場合に返します。`prompt=none` でなければ、この状況では認可サーバが
 * organization の選択画面を表示するため、クライアント側に選択 UI の実装は不要です
 */
export const OidcAuthorizeErrorCode = { LOGIN_REQUIRED: 'login_required', INTERACTION_REQUIRED: 'interaction_required' } as const;

/**
 * `/oauth/authorize` が 302 リダイレクトの `?error=` クエリパラメータに設定するエラーコード
 * (OIDC Core 1.0 Section 3.1.2.6)。
 *
 * RFC 6749 Section 4.1.2.1 が定義する他のエラー (`invalid_request` /
 * `unsupported_response_type` など) は本実装では 422 Problem Details として
 * `application/problem+json` 形式で返され、`OauthAuthorizeErrorCode` を参照してください。
 *
 * どちらも `prompt=none` が指定されたときにのみ返されます。`prompt=none` でなければ、
 * 対話が必要な状況では認可サーバがそのための画面を表示します。
 *
 * - `login_required`: 対話的な**認証**が必要だが `prompt=none` のため実行できない
 * (OIDC Core Section 3.1.2.6)。セッションが無い場合と、SSO 強制 organization に対して
 * 非 SSO セッションを再利用しようとした場合に返します
 * - `interaction_required`: ユーザーは認証済みだが、続行するには対話が必要
 * (OIDC Core Section 3.1.2.6)。active な organization に 2 件以上所属していて
 * `organization_id` の指定が無く、どの organization にトークンをバインドするか
 * 確定できない場合に返します。`prompt=none` でなければ、この状況では認可サーバが
 * organization の選択画面を表示するため、クライアント側に選択 UI の実装は不要です
 */
export type OidcAuthorizeErrorCode = typeof OidcAuthorizeErrorCode[keyof typeof OidcAuthorizeErrorCode];

/**
 * `/oauth/authorize` が 422 Problem Details (`application/problem+json`) のレスポンスボディで
 * `extensions.error` に設定する OAuth 2.0 エラーコード (RFC 6749 Section 4.1.2.1)。
 *
 * 本実装が実際に返す値のみ列挙しています。`invalid_scope` については `extensions.error` ではなく
 * より詳細な `extensions.scope_violation` 経由で通知されるため本 enum には含みません。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、または不正な形式 (PKCE S256 未指定、`state` / `nonce` 不正、組織指定不正など)
 * - `unsupported_response_type`: `response_type` が `code` 以外。本実装は Authorization Code フローのみサポート
 */
export const OauthAuthorizeErrorCode = { INVALID_REQUEST: 'invalid_request', UNSUPPORTED_RESPONSE_TYPE: 'unsupported_response_type' } as const;

/**
 * `/oauth/authorize` が 422 Problem Details (`application/problem+json`) のレスポンスボディで
 * `extensions.error` に設定する OAuth 2.0 エラーコード (RFC 6749 Section 4.1.2.1)。
 *
 * 本実装が実際に返す値のみ列挙しています。`invalid_scope` については `extensions.error` ではなく
 * より詳細な `extensions.scope_violation` 経由で通知されるため本 enum には含みません。
 *
 * - `invalid_request`: リクエストに必須パラメータが不足、無効なパラメータ値、または不正な形式 (PKCE S256 未指定、`state` / `nonce` 不正、組織指定不正など)
 * - `unsupported_response_type`: `response_type` が `code` 以外。本実装は Authorization Code フローのみサポート
 */
export type OauthAuthorizeErrorCode = typeof OauthAuthorizeErrorCode[keyof typeof OauthAuthorizeErrorCode];

/**
 * IdP (WorkOS) からの `/oauth/callback` 応答で `?error=` クエリパラメータに設定されるエラーコード
 * (RFC 6749 Section 4.1.2.1)。`/oauth/authorize` が本実装から返すエラーコード
 * (`OidcAuthorizeErrorCode` / `OauthAuthorizeErrorCode`) とは emit 元が異なるため別 enum として定義。
 *
 * - `access_denied`: ユーザーが認可を拒否した
 * - `server_error`: IdP でサーバーエラーが発生した
 * - `temporarily_unavailable`: IdP が一時的に利用不可
 * - `invalid_request`: リクエストが不正
 * - `unauthorized_client`: クライアントが認可されていない
 * - `unsupported_response_type`: `response_type` がサポートされていない
 * - `invalid_scope`: スコープが不正
 */
export const IdpAuthorizeErrorCode = {
    ACCESS_DENIED: 'access_denied',
    SERVER_ERROR: 'server_error',
    TEMPORARILY_UNAVAILABLE: 'temporarily_unavailable',
    INVALID_REQUEST: 'invalid_request',
    UNAUTHORIZED_CLIENT: 'unauthorized_client',
    UNSUPPORTED_RESPONSE_TYPE: 'unsupported_response_type',
    INVALID_SCOPE: 'invalid_scope'
} as const;

/**
 * IdP (WorkOS) からの `/oauth/callback` 応答で `?error=` クエリパラメータに設定されるエラーコード
 * (RFC 6749 Section 4.1.2.1)。`/oauth/authorize` が本実装から返すエラーコード
 * (`OidcAuthorizeErrorCode` / `OauthAuthorizeErrorCode`) とは emit 元が異なるため別 enum として定義。
 *
 * - `access_denied`: ユーザーが認可を拒否した
 * - `server_error`: IdP でサーバーエラーが発生した
 * - `temporarily_unavailable`: IdP が一時的に利用不可
 * - `invalid_request`: リクエストが不正
 * - `unauthorized_client`: クライアントが認可されていない
 * - `unsupported_response_type`: `response_type` がサポートされていない
 * - `invalid_scope`: スコープが不正
 */
export type IdpAuthorizeErrorCode = typeof IdpAuthorizeErrorCode[keyof typeof IdpAuthorizeErrorCode];

/**
 * バリデーションに失敗したパラメータ 1 件分の情報。
 * RFC 9457 の拡張メンバー `invalid_params` の要素として返却されます。
 */
export type InvalidParam = {
    /**
     * 失敗したパラメータ名。リクエストボディのフィールド名、クエリパラメータ名、パスパラメータ名のいずれか。
     */
    name: string;
    /**
     * 失敗理由の人間可読な説明。
     */
    reason: string;
};

/**
 * JSON Web Key (JWK) の基底スキーマ (RFC 7517)。
 * `kty` discriminator で具象サブクラス (RsaJsonWebKey / EcJsonWebKey) にルーティングされます。
 */
export type JsonWebKey = {
    /**
     * 鍵タイプ (Key Type)。
     * "RSA" または "EC" のいずれか。discriminator として使用されます。
     */
    kty: 'RSA' | 'EC';
    /**
     * 公開鍵の用途 (Public Key Use)。
     * 署名検証用の場合は "sig"。
     */
    use: 'sig';
    /**
     * 鍵 ID (Key ID)。
     * JWT ヘッダーの kid と照合して使用する鍵を特定します。
     */
    kid: string;
    /**
     * アルゴリズム (Algorithm)。
     * この鍵で使用するアルゴリズムを示します。
     */
    alg: 'RS256' | 'PS256' | 'ES256';
    [key: string]: unknown;
};

/**
 * RSA JSON Web Key (JWK)。
 * RSA 公開鍵を JSON 形式で表現します (RFC 7517)。
 */
export type RsaJsonWebKey = Omit<JsonWebKey, 'kty'> & {
    /**
     * RSA 公開鍵のモジュラス (Modulus)。
     * Base64url エンコードされた値。
     */
    n: string;
    /**
     * RSA 公開鍵の指数 (Exponent)。
     * Base64url エンコードされた値。
     */
    e: string;
    kty: 'RSA';
    [key: string]: unknown;
};

/**
 * EC JSON Web Key (JWK)。
 * 楕円曲線 (Elliptic Curve) 公開鍵を JSON 形式で表現します (RFC 7517)。
 */
export type EcJsonWebKey = Omit<JsonWebKey, 'kty'> & {
    /**
     * 楕円曲線名 (Curve)。
     * ES256 の場合は "P-256"。
     */
    crv: 'P-256';
    /**
     * 楕円曲線の x 座標 (X Coordinate)。
     * Base64url エンコードされた値。
     */
    x: string;
    /**
     * 楕円曲線の y 座標 (Y Coordinate)。
     * Base64url エンコードされた値。
     */
    y: string;
    kty: 'EC';
    [key: string]: unknown;
};

/**
 * OAuth 2.0 クライアント識別子（英数字小文字・ハイフン）。
 *
 * クライアントは事前に認可サーバーに登録され、一意の識別子が発行されます。
 * パブリッククライアント（SPA、モバイルアプリ）と機密クライアント（サーバーサイドアプリ）の両方で使用されます。
 */
export type ClientId = string;

/**
 * 認証後にリダイレクトする URI。登録済みのコールバック URI と一致する必要があります。
 *
 * **重要**: `/oauth/authorize` で使用した値と `/oauth/token` で送信する値は完全一致する必要があります（RFC 6749 Section 4.1.3）。
 * クライアントは事前に認可サーバーへリダイレクト URI を登録し、ホワイトリスト化されたもののみが許可されます。
 */
export type RedirectUri = string;

/**
 * 要求するスコープを空白区切りで指定します（例: `openid profile email`）。
 *
 * **許可されたスコープ値**:
 * - `openid`: OpenID Connect 認証（必須）
 * - `profile`: ユーザープロファイル情報へのアクセス
 * - `email`: メールアドレスへのアクセス
 * - `offline_access`: リフレッシュトークンの発行
 */
export type Scope = string;

/**
 * PKCE コードチャレンジメソッド。本実装は S256（SHA-256）のみをサポートします。
 *
 * **RFC 7636 Section 4.3**:
 * - `S256`: code_challenge = BASE64URL(SHA256(ASCII(code_verifier)))
 * - `plain`: セキュリティ上のリスクがあるため、本実装では非サポート
 *
 * クライアントは必ず S256 メソッドを使用してください。
 */
export const CodeChallengeMethod = { S256: 'S256' } as const;

/**
 * PKCE コードチャレンジメソッド。本実装は S256（SHA-256）のみをサポートします。
 *
 * **RFC 7636 Section 4.3**:
 * - `S256`: code_challenge = BASE64URL(SHA256(ASCII(code_verifier)))
 * - `plain`: セキュリティ上のリスクがあるため、本実装では非サポート
 *
 * クライアントは必ず S256 メソッドを使用してください。
 */
export type CodeChallengeMethod2 = typeof CodeChallengeMethod[keyof typeof CodeChallengeMethod];

/**
 * PKCE (RFC 7636) で使用する文字列。unreserved 文字セット（A-Z, a-z, 0-9, -, ., _, ~）のみで構成されます。
 *
 * **RFC 7636 Section 4.1**:
 * > unreserved = ALPHA / DIGIT / "-" / "." / "_" / "~"
 *
 * このスキーマは code_challenge と code_verifier の両方に使用されます。
 */
export type PkceVerifierString = string;

/**
 * OIDC 認証プロンプト制御 (OIDC Core 1.0 Section 3.1.2.1 の subset)。
 * - `none`: ユーザーインタラクションなしで認証を試みる。セッションがない場合は `login_required` エラーをリダイレクト
 * - `login`: 既存セッションを無視して再認証を強制
 * - 未指定: セッションがあれば利用、なければ IdP リダイレクト
 */
export const Prompt = { NONE: 'none', LOGIN: 'login' } as const;

/**
 * OIDC 認証プロンプト制御 (OIDC Core 1.0 Section 3.1.2.1 の subset)。
 * - `none`: ユーザーインタラクションなしで認証を試みる。セッションがない場合は `login_required` エラーをリダイレクト
 * - `login`: 既存セッションを無視して再認証を強制
 * - 未指定: セッションがあれば利用、なければ IdP リダイレクト
 */
export type Prompt2 = typeof Prompt[keyof typeof Prompt];

/**
 * 組織招待一覧レスポンス（ページネーション付き）。
 */
export type AdminOrganizationInvitationListResponse = {
    /**
     * 招待の配列。
     */
    data: Array<OrganizationInvitation>;
    meta: PaginationMeta;
};

/**
 * 組織情報更新リクエスト（組織メンバー向け）。
 * 指定されたフィールドのみが更新されます。
 *
 * user-facing 版は `display_name` のみ更新可能です。
 * `name` (slug) / `is_active` / `is_sso_enforced` は管理者専用エンドポイント
 * (`PATCH /admin/organizations/{organization_id}`) からのみ更新できます。
 */
export type OrganizationUpdateRequest = {
    /**
     * 組織のヘッダー表示用の人間向け名称。
     */
    display_name?: string;
};

/**
 * OAuth 2.0 Authorization Code フローを示す固定値です。
 */
export type ResponseType = ResponseType2;

export type ClientId2 = ClientId;

export type RedirectUri2 = RedirectUri;

export type Scope2 = Scope;

/**
 * CSRF 対策用のランダムなトークン。
 */
export type AuthorizeState = string;

export type CodeChallengeMethod = CodeChallengeMethod2;

export type CodeChallenge = PkceVerifierString & unknown;

/**
 * OIDC リプレイ攻撃対策用の値。レスポンスに含めて検証します。
 */
export type Nonce = string;

/**
 * 認可対象の組織を指定する組織 ID（認可サーバが発行する UUID）。
 *
 * IdP リダイレクトを伴うフローでは、指定された組織は組織固有の SSO プロバイダ（Okta, Azure AD 等）へのルーティングに使用されます。存在しない、非アクティブ、または IdP 未連携の組織 ID を指定した場合はエラーになります。
 *
 * 未指定時は認可サーバが下記の順序で組織を解決します:
 * - 認可サーバ側でユーザが active な組織に対して active なメンバーシップをちょうど 1 つだけ持つ場合、その組織に自動的にバインドして `org_id` クレーム付きの id_token を発行します（silent SSO / 新規ログイン両フロー）。
 * - 上記に該当しない場合（複数組織所属 / 組織未所属）は `org_id` クレーム無しの id_token が発行されます。
 *
 */
export type OrganizationId = string;

/**
 * WorkOS 招待メールに含まれるトークン。指定時は WorkOS AuthKit に招待受諾フローとしてパススルーされます。
 */
export type InvitationToken = string;

export type Prompt = Prompt2;

/**
 * WorkOS AuthKit の Sign-in endpoint から渡される不透明トークン。招待受諾やパスワードリセット等、アプリ外から開始されたフローのコンテキストを保持します。
 */
export type Context = string;

/**
 * IdP から発行された認可コード。
 * 認証が成功した場合にのみ含まれます。
 */
export type Code = string;

/**
 * `/oauth/authorize` リクエスト時にクライアントが送信した state パラメータ。
 * CSRF 対策およびリクエストの紐付けに使用します。
 */
export type CallbackState = string;

/**
 * IdP での認証・認可が失敗した場合のエラーコード (RFC 6749 Section 4.1.2.1)。
 */
export type Error = IdpAuthorizeErrorCode;

/**
 * エラーの詳細を説明する人間可読なテキスト (RFC 6749 Section 4.1.2.1)。
 */
export type ErrorDescription = string;

/**
 * ログアウト対象のユーザーを特定するための ID トークン。
 *
 * OP (OpenID Provider) は、このトークンを使用してログアウト対象の
 * エンドユーザーのセッションを特定します。トークンは暗号化されていても構いません。
 *
 * OP は必ずしもトークンの署名を検証する必要はありませんが、
 * `aud` クレームに自身が含まれていることを確認します。
 */
export type IdTokenHint = string;

/**
 * ログアウト対象のエンドユーザーに関するヒント。
 *
 * OP がエンドユーザーを特定するために使用できる追加情報です。
 * `id_token_hint` が提供されていない場合に、ユーザーを特定するための
 * 補助的な手段として使用されることがあります。
 */
export type LogoutHint = string;

/**
 * ログアウト完了後にユーザーをリダイレクトする URI。
 *
 * この URI は、事前にクライアント登録時に `post_logout_redirect_uris` として
 * 登録されている必要があります。登録されていない URI が指定された場合、
 * リダイレクトは行われません。
 *
 * `id_token_hint` が省略され、`client_id` が指定されている場合、
 * その `client_id` に登録されている URI と照合されます。
 *
 * `id_token_hint` と `client_id` の両方が省略された場合、
 * リダイレクトは行われず、ログアウト完了画面が表示されます。
 */
export type PostLogoutRedirectUri = string;

/**
 * RP (Relying Party) のクライアント識別子。
 *
 * `id_token_hint` が提供されない場合に、クライアントを特定するために使用されます。
 * `id_token_hint` が提供される場合、このパラメータは無視される場合があります。
 */
export type EndSessionClientId = ClientId;

/**
 * RP がセッション状態を維持するために生成する推測困難な任意の文字列（OP はこの値を解釈せずそのまま返却します）。
 *
 * `post_logout_redirect_uri` へのリダイレクト時にクエリパラメータとして
 * そのまま返却されます。CSRF 攻撃を防ぐために使用できます。
 */
export type EndSessionState = string;

/**
 * エンドユーザーの優先言語およびスクリプトを表す BCP47 言語タグのスペース区切りリスト。
 *
 * OP はこの値を使用して、ログアウト確認画面やエラーメッセージの表示言語を
 * 決定することができます。優先度の高い順に並べます。
 */
export type UiLocales = string;

/**
 * 取得するページ番号（1始まり）。
 */
export type Page = number;

/**
 * 1ページあたりの取得件数。
 */
export type PerPage = number;

/**
 * クライアント識別子。
 */
export type ClientIdPath = string;

/**
 * 組織識別子（UUID形式）。
 */
export type OrganizationIdPath = string;

/**
 * ソート基準フィールド。
 * - `name`: ユーザー表示名
 * - `email`: ユーザーメールアドレス
 * - `role`: 組織内ロール
 * - `joined_at`: 組織への参加日時
 * - `last_access`: 組織への最終アクセス日時
 */
export const MemberSortBy = {
    NAME: 'name',
    EMAIL: 'email',
    ROLE: 'role',
    JOINED_AT: 'joined_at',
    LAST_ACCESS: 'last_access'
} as const;

/**
 * ソート基準フィールド。
 * - `name`: ユーザー表示名
 * - `email`: ユーザーメールアドレス
 * - `role`: 組織内ロール
 * - `joined_at`: 組織への参加日時
 * - `last_access`: 組織への最終アクセス日時
 */
export type MemberSortBy = typeof MemberSortBy[keyof typeof MemberSortBy];

/**
 * ソート順。
 * - `asc`: 昇順
 * - `desc`: 降順
 */
export const MemberSortOrder = { ASC: 'asc', DESC: 'desc' } as const;

/**
 * ソート順。
 * - `asc`: 昇順
 * - `desc`: 降順
 */
export type MemberSortOrder = typeof MemberSortOrder[keyof typeof MemberSortOrder];

/**
 * ロールでメンバーを絞り込みます。カンマ区切りで複数値を指定可能（例: `?role=owner,admin`）。
 * 未指定時は全ロールを返却します。
 */
export type MemberRoleFilter = Array<OrganizationRole>;

/**
 * メンバーのメールドメインが組織の verified ドメインと一致するか (`corporate`)、しないか (`personal`) で絞り込みます。
 * カンマ区切りで複数値を指定可能（例: `?domain_type=corporate,personal`）。未指定時は全件返却します。
 */
export type MemberDomainTypeFilter = Array<OrganizationMemberDomainType>;

/**
 * メンバーの表示名またはメールアドレスに対する部分一致検索文字列。
 * 1〜255 文字。空文字列はバリデーションエラー（400）となります。
 */
export type MemberSearch = string;

/**
 * `true` の場合、レスポンスのトップレベルに `role_counts` と `domain_counts`（フィルタ適用前の総件数集計）を含めて返却します。
 * 集計コストを避けるためデフォルトは `false`。
 */
export type IncludeCounts = boolean;

/**
 * メンバー識別子（UUID形式）。
 */
export type MemberIdPath = string;

/**
 * 招待識別子（UUID形式）。
 */
export type InvitationIdPath = string;

export type GetServiceInfoData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/';
};

export type GetServiceInfoErrors = {
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    404: ProblemDetails;
};

export type GetServiceInfoError = GetServiceInfoErrors[keyof GetServiceInfoErrors];

export type GetServiceInfoResponses = {
    /**
     * サービス情報の応答。
     */
    200: ServiceInfoResponse;
};

export type GetServiceInfoResponse = GetServiceInfoResponses[keyof GetServiceInfoResponses];

export type GetOpenIdConfigurationData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/.well-known/openid-configuration';
};

export type GetOpenIdConfigurationErrors = {
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type GetOpenIdConfigurationError = GetOpenIdConfigurationErrors[keyof GetOpenIdConfigurationErrors];

export type GetOpenIdConfigurationResponses = {
    /**
     * OpenID Connect プロバイダーメタデータ。
     */
    200: OpenIdProviderMetadataResponse;
};

export type GetOpenIdConfigurationResponse = GetOpenIdConfigurationResponses[keyof GetOpenIdConfigurationResponses];

export type GetHealthStatusData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/health';
};

export type GetHealthStatusErrors = {
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type GetHealthStatusError = GetHealthStatusErrors[keyof GetHealthStatusErrors];

export type GetHealthStatusResponses = {
    /**
     * ヘルスチェックが成功した場合の応答。
     */
    200: HealthStatus;
};

export type GetHealthStatusResponse = GetHealthStatusResponses[keyof GetHealthStatusResponses];

export type GetJwksData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/jwks';
};

export type GetJwksErrors = {
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type GetJwksError = GetJwksErrors[keyof GetJwksErrors];

export type GetJwksResponses = {
    /**
     * JSON Web Key Set の取得に成功した場合のレスポンス。
     */
    200: JwksResponse;
};

export type GetJwksResponse = GetJwksResponses[keyof GetJwksResponses];

export type InitiateAuthorizationData = {
    body?: never;
    path?: never;
    query: {
        /**
         * OAuth 2.0 Authorization Code フローを示す固定値です。
         */
        response_type: ResponseType2;
        client_id: ClientId;
        redirect_uri: RedirectUri;
        scope: Scope;
        /**
         * CSRF 対策用のランダムなトークン。
         */
        state: string;
        code_challenge_method: CodeChallengeMethod2;
        code_challenge: PkceVerifierString & unknown;
        /**
         * OIDC リプレイ攻撃対策用の値。レスポンスに含めて検証します。
         */
        nonce: string;
        /**
         * 認可対象の組織を指定する組織 ID（認可サーバが発行する UUID）。
         *
         * IdP リダイレクトを伴うフローでは、指定された組織は組織固有の SSO プロバイダ（Okta, Azure AD 等）へのルーティングに使用されます。存在しない、非アクティブ、または IdP 未連携の組織 ID を指定した場合はエラーになります。
         *
         * 未指定時は認可サーバが下記の順序で組織を解決します:
         * - 認可サーバ側でユーザが active な組織に対して active なメンバーシップをちょうど 1 つだけ持つ場合、その組織に自動的にバインドして `org_id` クレーム付きの id_token を発行します（silent SSO / 新規ログイン両フロー）。
         * - 上記に該当しない場合（複数組織所属 / 組織未所属）は `org_id` クレーム無しの id_token が発行されます。
         *
         */
        organization_id?: string;
        /**
         * WorkOS 招待メールに含まれるトークン。指定時は WorkOS AuthKit に招待受諾フローとしてパススルーされます。
         */
        invitation_token?: string;
        prompt?: Prompt2;
        /**
         * WorkOS AuthKit の Sign-in endpoint から渡される不透明トークン。招待受諾やパスワードリセット等、アプリ外から開始されたフローのコンテキストを保持します。
         */
        context?: string;
    };
    url: '/oauth/authorize';
};

export type InitiateAuthorizationErrors = {
    /**
     * エラー情報を含むHTMLページ
     */
    400: string;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type InitiateAuthorizationError = InitiateAuthorizationErrors[keyof InitiateAuthorizationErrors];

export type HandleIdpCallbackData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * IdP から発行された認可コード。
         * 認証が成功した場合にのみ含まれます。
         */
        code?: string;
        /**
         * `/oauth/authorize` リクエスト時にクライアントが送信した state パラメータ。
         * CSRF 対策およびリクエストの紐付けに使用します。
         */
        state?: string;
        /**
         * IdP での認証・認可が失敗した場合のエラーコード (RFC 6749 Section 4.1.2.1)。
         */
        error?: IdpAuthorizeErrorCode;
        /**
         * エラーの詳細を説明する人間可読なテキスト (RFC 6749 Section 4.1.2.1)。
         */
        error_description?: string;
    };
    url: '/oauth/callback';
};

export type HandleIdpCallbackErrors = {
    /**
     * エラー情報を含むHTMLページ
     */
    400: string;
    /**
     * エラー情報を含むHTMLページ
     */
    403: string;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type HandleIdpCallbackError = HandleIdpCallbackErrors[keyof HandleIdpCallbackErrors];

export type IssueTokensData = {
    body: TokenRequest;
    path?: never;
    query?: never;
    url: '/oauth/token';
};

export type IssueTokensErrors = {
    /**
     * トークンリクエストが無効な場合のエラーレスポンス。
     *
     * このステータスは 2 つの形式を取り、`Content-Type` で区別されます。
     *
     * **`application/json`** — 認可サーバーが処理したうえで返す OAuth 2.0 エラー (RFC 6749 Section 5.2)。
     * - `invalid_request`: リクエストパラメータの不備
     * - `invalid_grant`: 認可コード/リフレッシュトークンの問題
     * - `unauthorized_client`: グラントタイプの権限不足
     * - `unsupported_grant_type`: 未サポートのグラントタイプ
     * - `invalid_scope`: スコープの問題
     *
     * **`application/problem+json`** — リクエストが処理前のバリデーションで棄却された場合の
     * RFC 9457 Problem Details (`type: .../problems/validation-error`)。必須パラメータの欠落、
     * 型・形式違反、パラメータの排他制約違反などが該当します。
     */
    400: TokenErrorResponse;
    /**
     * クライアント認証に失敗した場合のエラーレスポンス (RFC 6749 Section 5.2)。
     *
     * **返却されるエラーコード**:
     * - `invalid_client`: クライアント認証失敗
     *
     * HTTP Basic 認証を使用している場合、WWW-Authenticate ヘッダーが含まれます。
     */
    401: TokenErrorResponse;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type IssueTokensError = IssueTokensErrors[keyof IssueTokensErrors];

export type IssueTokensResponses = {
    /**
     * トークンの発行に成功した場合のレスポンス。
     */
    200: TokenResponse;
};

export type IssueTokensResponse = IssueTokensResponses[keyof IssueTokensResponses];

export type GetUserinfoData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/oauth/userinfo';
};

export type GetUserinfoErrors = {
    /**
     * アクセストークンが無効または期限切れの場合のエラーレスポンス (RFC 6750)。
     */
    401: {
        /**
         * エラーコード。
         */
        error: 'invalid_token';
        /**
         * エラーの詳細な説明。
         */
        error_description?: string;
    };
    /**
     * アクセストークンに必要なスコープが含まれていない場合のエラーレスポンス (RFC 6750)。
     */
    403: {
        /**
         * エラーコード。
         */
        error: 'insufficient_scope';
        /**
         * エラーの詳細な説明。
         */
        error_description?: string;
    };
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type GetUserinfoError = GetUserinfoErrors[keyof GetUserinfoErrors];

export type GetUserinfoResponses = {
    /**
     * ユーザー情報の取得に成功した場合のレスポンス。
     */
    200: UserinfoResponse;
};

export type GetUserinfoResponse = GetUserinfoResponses[keyof GetUserinfoResponses];

export type PostUserinfoData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/oauth/userinfo';
};

export type PostUserinfoErrors = {
    /**
     * アクセストークンが無効または期限切れの場合のエラーレスポンス (RFC 6750)。
     */
    401: {
        /**
         * エラーコード。
         */
        error: 'invalid_token';
        /**
         * エラーの詳細な説明。
         */
        error_description?: string;
    };
    /**
     * アクセストークンに必要なスコープが含まれていない場合のエラーレスポンス (RFC 6750)。
     */
    403: {
        /**
         * エラーコード。
         */
        error: 'insufficient_scope';
        /**
         * エラーの詳細な説明。
         */
        error_description?: string;
    };
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type PostUserinfoError = PostUserinfoErrors[keyof PostUserinfoErrors];

export type PostUserinfoResponses = {
    /**
     * ユーザー情報の取得に成功した場合のレスポンス。
     */
    200: UserinfoResponse;
};

export type PostUserinfoResponse = PostUserinfoResponses[keyof PostUserinfoResponses];

export type RevokeTokenData = {
    body: RevokeRequest;
    path?: never;
    query?: never;
    url: '/oauth/revoke';
};

export type RevokeTokenErrors = {
    /**
     * トークン無効化リクエストがバリデーションで棄却された場合のエラーレスポンス。
     *
     * RFC 9457 Problem Details (`type: .../problems/validation-error`) を
     * `application/problem+json` で返却します。`token` の欠落・空文字・空白のみ、
     * `token_type_hint` が列挙値以外、などが該当します。
     *
     * RFC 7009 Section 2.2.1 形式の OAuth 2.0 エラー (`application/json`) は
     * このステータスでは返却されません。`token` / `token_type_hint` の不備は
     * すべて FormRequest で棄却されるため、ドメイン層の `invalid_request` /
     * `unsupported_token_type` に到達する経路が存在しないためです。
     * クライアント認証失敗時の `invalid_client` は 401 で返却されます。
     */
    400: ProblemDetails;
    /**
     * クライアント認証に失敗した場合のエラーレスポンス (RFC 7009 Section 2.1)。
     *
     * **返却されるエラーコード**:
     * - `invalid_client`: クライアント認証失敗
     *
     * HTTP Basic 認証を使用している場合、WWW-Authenticate ヘッダーが含まれます。
     */
    401: RevokeErrorResponse;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type RevokeTokenError = RevokeTokenErrors[keyof RevokeTokenErrors];

export type RevokeTokenResponses = {
    /**
     * トークンの無効化に成功した場合のレスポンス。
     *
     * RFC 7009 Section 2.2 に従い、無効なトークン（既に失効済み、存在しない等）を
     * 無効化しようとした場合も HTTP 200 が返されます。これは、クライアントに
     * トークンの存在有無を漏洩させないためのセキュリティ上の設計です。
     */
    200: unknown;
};

export type IntrospectTokenData = {
    body: IntrospectRequest;
    path?: never;
    query?: never;
    url: '/oauth/introspect';
};

export type IntrospectTokenErrors = {
    /**
     * トークンイントロスペクションリクエストがバリデーションで棄却された場合のエラーレスポンス。
     *
     * RFC 9457 Problem Details (`type: .../problems/validation-error`) を
     * `application/problem+json` で返却します。`token` の欠落・空文字・空白のみ、
     * `token_type_hint` が列挙値以外、などが該当します。
     *
     * RFC 7662 Section 2.3 形式の OAuth 2.0 エラー (`application/json`) は
     * このステータスでは返却されません。`token` / `token_type_hint` の不備は
     * すべて FormRequest で棄却されるため、ドメイン層の `invalid_request` に
     * 到達する経路が存在しないためです。
     * クライアント認証失敗時の `invalid_client` は 401 で返却されます。
     */
    400: ProblemDetails;
    /**
     * クライアント認証に失敗した場合のエラーレスポンス (RFC 7662 Section 2.1)。
     *
     * **返却されるエラーコード**:
     * - `invalid_client`: クライアント認証失敗
     *
     * HTTP Basic 認証を使用している場合、WWW-Authenticate ヘッダーが含まれます。
     *
     * **注意**: トークンイントロスペクションエンドポイントは、トークンスキャン攻撃を防ぐため、
     * 認証なしでのアクセスを許可しません。リソースサーバーは必ず認証情報を提供する必要があります。
     */
    401: IntrospectErrorResponse;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type IntrospectTokenError = IntrospectTokenErrors[keyof IntrospectTokenErrors];

export type IntrospectTokenResponses = {
    /**
     * トークンイントロスペクションの結果。
     *
     * RFC 7662 Section 2.2 に従い、トークンが無効（期限切れ、失効済み、存在しない等）な場合でも
     * HTTP 200 が返され、`active: false` がレスポンスに含まれます。
     * これは、トークンの存在有無を漏洩させないためのセキュリティ上の設計です。
     */
    200: IntrospectResponse;
};

export type IntrospectTokenResponse = IntrospectTokenResponses[keyof IntrospectTokenResponses];

export type EndSessionData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * ログアウト対象のユーザーを特定するための ID トークン。
         *
         * OP (OpenID Provider) は、このトークンを使用してログアウト対象の
         * エンドユーザーのセッションを特定します。トークンは暗号化されていても構いません。
         *
         * OP は必ずしもトークンの署名を検証する必要はありませんが、
         * `aud` クレームに自身が含まれていることを確認します。
         */
        id_token_hint?: string;
        /**
         * ログアウト対象のエンドユーザーに関するヒント。
         *
         * OP がエンドユーザーを特定するために使用できる追加情報です。
         * `id_token_hint` が提供されていない場合に、ユーザーを特定するための
         * 補助的な手段として使用されることがあります。
         */
        logout_hint?: string;
        /**
         * ログアウト完了後にユーザーをリダイレクトする URI。
         *
         * この URI は、事前にクライアント登録時に `post_logout_redirect_uris` として
         * 登録されている必要があります。登録されていない URI が指定された場合、
         * リダイレクトは行われません。
         *
         * `id_token_hint` が省略され、`client_id` が指定されている場合、
         * その `client_id` に登録されている URI と照合されます。
         *
         * `id_token_hint` と `client_id` の両方が省略された場合、
         * リダイレクトは行われず、ログアウト完了画面が表示されます。
         */
        post_logout_redirect_uri?: string;
        /**
         * RP (Relying Party) のクライアント識別子。
         *
         * `id_token_hint` が提供されない場合に、クライアントを特定するために使用されます。
         * `id_token_hint` が提供される場合、このパラメータは無視される場合があります。
         */
        client_id?: ClientId;
        /**
         * RP がセッション状態を維持するために生成する推測困難な任意の文字列（OP はこの値を解釈せずそのまま返却します）。
         *
         * `post_logout_redirect_uri` へのリダイレクト時にクエリパラメータとして
         * そのまま返却されます。CSRF 攻撃を防ぐために使用できます。
         */
        state?: string;
        /**
         * エンドユーザーの優先言語およびスクリプトを表す BCP47 言語タグのスペース区切りリスト。
         *
         * OP はこの値を使用して、ログアウト確認画面やエラーメッセージの表示言語を
         * 決定することができます。優先度の高い順に並べます。
         */
        ui_locales?: string;
    };
    url: '/oauth/logout';
};

export type EndSessionErrors = {
    /**
     * エラー情報を含むHTMLページ
     */
    400: string;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type EndSessionError = EndSessionErrors[keyof EndSessionErrors];

export type EndSessionResponses = {
    /**
     * ログアウト完了画面の HTML
     */
    200: string;
};

export type EndSessionResponse = EndSessionResponses[keyof EndSessionResponses];

export type EndSessionPostData = {
    body?: EndSessionRequest;
    path?: never;
    query?: never;
    url: '/oauth/logout';
};

export type EndSessionPostErrors = {
    /**
     * エラー情報を含むHTMLページ
     */
    400: string;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    503: ProblemDetails;
};

export type EndSessionPostError = EndSessionPostErrors[keyof EndSessionPostErrors];

export type EndSessionPostResponses = {
    /**
     * ログアウト完了画面の HTML
     */
    200: string;
};

export type EndSessionPostResponse = EndSessionPostResponses[keyof EndSessionPostResponses];

export type ListClientsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * 取得するページ番号（1始まり）。
         */
        page?: number;
        /**
         * 1ページあたりの取得件数。
         */
        per_page?: number;
    };
    url: '/admin/clients';
};

export type ListClientsErrors = {
    /**
     * クエリパラメータのバリデーションエラーが発生した場合のエラーレスポンス。
     * - `page` が整数でない、または 1 未満
     * - `per_page` が整数でない、1 未満、または 100 超
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type ListClientsError = ListClientsErrors[keyof ListClientsErrors];

export type ListClientsResponses = {
    /**
     * クライアント一覧の取得に成功した場合のレスポンス。
     */
    200: AdminClientListResponse;
};

export type ListClientsResponse = ListClientsResponses[keyof ListClientsResponses];

export type CreateClientData = {
    body: AdminClientCreateRequest;
    path?: never;
    query?: never;
    url: '/admin/clients';
};

export type CreateClientErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type CreateClientError = CreateClientErrors[keyof CreateClientErrors];

export type CreateClientResponses = {
    /**
     * クライアントの登録に成功した場合のレスポンス。
     */
    201: AdminClientCreatedResponse;
};

export type CreateClientResponse = CreateClientResponses[keyof CreateClientResponses];

export type DeleteClientData = {
    body?: never;
    path: {
        /**
         * クライアント識別子。
         */
        client_id: string;
    };
    query?: never;
    url: '/admin/clients/{client_id}';
};

export type DeleteClientErrors = {
    /**
     * パスパラメータのバリデーションエラーが発生した場合のエラーレスポンス。
     * - `client_id` が 8 文字未満、128 文字超、または `^[a-z0-9-]+$` に一致しない
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定されたクライアントが存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type DeleteClientError = DeleteClientErrors[keyof DeleteClientErrors];

export type DeleteClientResponses = {
    /**
     * クライアントの削除に成功した場合のレスポンス（ボディなし）。
     */
    204: void;
};

export type DeleteClientResponse = DeleteClientResponses[keyof DeleteClientResponses];

export type GetClientData = {
    body?: never;
    path: {
        /**
         * クライアント識別子。
         */
        client_id: string;
    };
    query?: never;
    url: '/admin/clients/{client_id}';
};

export type GetClientErrors = {
    /**
     * パスパラメータのバリデーションエラーが発生した場合のエラーレスポンス。
     * - `client_id` が 8 文字未満、128 文字超、または `^[a-z0-9-]+$` に一致しない
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定されたクライアントが存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type GetClientError = GetClientErrors[keyof GetClientErrors];

export type GetClientResponses = {
    /**
     * クライアント情報の取得に成功した場合のレスポンス。
     */
    200: Client;
};

export type GetClientResponse = GetClientResponses[keyof GetClientResponses];

export type UpdateClientData = {
    body: AdminClientUpdateRequest;
    path: {
        /**
         * クライアント識別子。
         */
        client_id: string;
    };
    query?: never;
    url: '/admin/clients/{client_id}';
};

export type UpdateClientErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定されたクライアントが存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type UpdateClientError = UpdateClientErrors[keyof UpdateClientErrors];

export type UpdateClientResponses = {
    /**
     * クライアントの更新に成功した場合のレスポンス。
     */
    200: Client;
};

export type UpdateClientResponse = UpdateClientResponses[keyof UpdateClientResponses];

export type ListOrganizationsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * 取得するページ番号（1始まり）。
         */
        page?: number;
        /**
         * 1ページあたりの取得件数。
         */
        per_page?: number;
    };
    url: '/admin/organizations';
};

export type ListOrganizationsErrors = {
    /**
     * クエリパラメータのバリデーションエラーが発生した場合のエラーレスポンス。
     * - `page` が整数でない、または 1 未満
     * - `per_page` が整数でない、1 未満、または 100 超
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type ListOrganizationsError = ListOrganizationsErrors[keyof ListOrganizationsErrors];

export type ListOrganizationsResponses = {
    /**
     * 組織一覧の取得に成功した場合のレスポンス。
     */
    200: AdminOrganizationListResponse;
};

export type ListOrganizationsResponse = ListOrganizationsResponses[keyof ListOrganizationsResponses];

export type CreateOrganizationData = {
    body: AdminOrganizationCreateRequest;
    path?: never;
    query?: never;
    url: '/admin/organizations';
};

export type CreateOrganizationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 管理者権限がない場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type CreateOrganizationError = CreateOrganizationErrors[keyof CreateOrganizationErrors];

export type CreateOrganizationResponses = {
    /**
     * 組織の登録に成功した場合のレスポンス。
     */
    201: AdminOrganizationCreatedResponse;
};

export type CreateOrganizationResponse = CreateOrganizationResponses[keyof CreateOrganizationResponses];

export type GetOrganizationData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}';
};

export type GetOrganizationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type GetOrganizationError = GetOrganizationErrors[keyof GetOrganizationErrors];

export type GetOrganizationResponses = {
    /**
     * 組織情報の取得に成功した場合のレスポンス。
     */
    200: Organization;
};

export type GetOrganizationResponse = GetOrganizationResponses[keyof GetOrganizationResponses];

export type UpdateOrganizationData = {
    body: AdminOrganizationUpdateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}';
};

export type UpdateOrganizationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     * 一意制約違反（name が既に使用されている場合）もこのエラーとして返されます。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type UpdateOrganizationError = UpdateOrganizationErrors[keyof UpdateOrganizationErrors];

export type UpdateOrganizationResponses = {
    /**
     * 組織の更新に成功した場合のレスポンス。
     */
    200: Organization;
};

export type UpdateOrganizationResponse = UpdateOrganizationResponses[keyof UpdateOrganizationResponses];

export type ListOrganizationMembersData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: {
        /**
         * 取得するページ番号（1始まり）。
         */
        page?: number;
        /**
         * 1ページあたりの取得件数。
         */
        per_page?: number;
        /**
         * ソート基準フィールド。
         * - `name`: ユーザー表示名
         * - `email`: ユーザーメールアドレス
         * - `role`: 組織内ロール
         * - `joined_at`: 組織への参加日時
         * - `last_access`: 組織への最終アクセス日時
         */
        sort_by?: 'name' | 'email' | 'role' | 'joined_at' | 'last_access';
        /**
         * ソート順。
         * - `asc`: 昇順
         * - `desc`: 降順
         */
        sort_order?: 'asc' | 'desc';
        /**
         * ロールでメンバーを絞り込みます。カンマ区切りで複数値を指定可能（例: `?role=owner,admin`）。
         * 未指定時は全ロールを返却します。
         */
        role?: Array<OrganizationRole>;
        /**
         * メンバーのメールドメインが組織の verified ドメインと一致するか (`corporate`)、しないか (`personal`) で絞り込みます。
         * カンマ区切りで複数値を指定可能（例: `?domain_type=corporate,personal`）。未指定時は全件返却します。
         */
        domain_type?: Array<OrganizationMemberDomainType>;
        /**
         * メンバーの表示名またはメールアドレスに対する部分一致検索文字列。
         * 1〜255 文字。空文字列はバリデーションエラー（400）となります。
         */
        search?: string;
        /**
         * `true` の場合、レスポンスのトップレベルに `role_counts` と `domain_counts`（フィルタ適用前の総件数集計）を含めて返却します。
         * 集計コストを避けるためデフォルトは `false`。
         */
        include_counts?: boolean;
    };
    url: '/admin/organizations/{organization_id}/members';
};

export type ListOrganizationMembersErrors = {
    /**
     * クエリパラメータが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type ListOrganizationMembersError = ListOrganizationMembersErrors[keyof ListOrganizationMembersErrors];

export type ListOrganizationMembersResponses = {
    /**
     * 組織メンバー一覧の取得に成功した場合のレスポンス。
     */
    200: AdminOrganizationMemberListResponse;
};

export type ListOrganizationMembersResponse = ListOrganizationMembersResponses[keyof ListOrganizationMembersResponses];

export type RemoveOrganizationMemberData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
        /**
         * メンバー識別子（UUID形式）。
         */
        member_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}/members/{member_id}';
};

export type RemoveOrganizationMemberErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織またはメンバーが存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * メンバーを削除できない状態の場合のエラーレスポンス。
     * - 組織内で唯一の Owner であるメンバーは削除できません
     */
    409: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type RemoveOrganizationMemberError = RemoveOrganizationMemberErrors[keyof RemoveOrganizationMemberErrors];

export type RemoveOrganizationMemberResponses = {
    /**
     * メンバーの削除に成功した場合のレスポンス（ボディなし）。
     */
    204: void;
};

export type RemoveOrganizationMemberResponse = RemoveOrganizationMemberResponses[keyof RemoveOrganizationMemberResponses];

export type UpdateOrganizationMemberRoleData = {
    body: AdminOrganizationMemberUpdateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
        /**
         * メンバー識別子（UUID形式）。
         */
        member_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}/members/{member_id}';
};

export type UpdateOrganizationMemberRoleErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織またはメンバーが存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * 競合が発生した場合のエラーレスポンス。
     * - 組織内の最後の Owner のロールを変更しようとした場合
     */
    409: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type UpdateOrganizationMemberRoleError = UpdateOrganizationMemberRoleErrors[keyof UpdateOrganizationMemberRoleErrors];

export type UpdateOrganizationMemberRoleResponses = {
    /**
     * メンバーロールの変更に成功した場合のレスポンス。
     */
    200: OrganizationMember;
};

export type UpdateOrganizationMemberRoleResponse = UpdateOrganizationMemberRoleResponses[keyof UpdateOrganizationMemberRoleResponses];

export type ListOrganizationInvitationsData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: {
        /**
         * 取得するページ番号（1始まり）。
         */
        page?: number;
        /**
         * 1ページあたりの取得件数。
         */
        per_page?: number;
    };
    url: '/admin/organizations/{organization_id}/invitations';
};

export type ListOrganizationInvitationsErrors = {
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     * - パスパラメータ `organization_id` が UUID 形式でない
     * - `page` が整数でない、または 1 未満
     * - `per_page` が整数でない、1 未満、または 100 超
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type ListOrganizationInvitationsError = ListOrganizationInvitationsErrors[keyof ListOrganizationInvitationsErrors];

export type ListOrganizationInvitationsResponses = {
    /**
     * 招待一覧の取得に成功した場合のレスポンス。
     */
    200: AdminOrganizationInvitationListResponse;
};

export type ListOrganizationInvitationsResponse = ListOrganizationInvitationsResponses[keyof ListOrganizationInvitationsResponses];

export type CreateOrganizationInvitationData = {
    body: AdminOrganizationInvitationCreateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}/invitations';
};

export type CreateOrganizationInvitationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * 競合が発生した場合のエラーレスポンス。
     * - 既に同じメールアドレスへの有効な招待が存在する場合
     * - 既に組織のメンバーである場合
     */
    409: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type CreateOrganizationInvitationError = CreateOrganizationInvitationErrors[keyof CreateOrganizationInvitationErrors];

export type CreateOrganizationInvitationResponses = {
    /**
     * 招待の作成に成功した場合のレスポンス。
     */
    201: AdminOrganizationInvitationCreatedResponse;
};

export type CreateOrganizationInvitationResponse = CreateOrganizationInvitationResponses[keyof CreateOrganizationInvitationResponses];

export type RevokeOrganizationInvitationData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
        /**
         * 招待識別子（UUID形式）。
         */
        invitation_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}/invitations/{invitation_id}';
};

export type RevokeOrganizationInvitationErrors = {
    /**
     * パスパラメータのバリデーションエラーが発生した場合のエラーレスポンス。
     * - `organization_id` または `invitation_id` が UUID 形式でない
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織または招待が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * 招待を取り消せない状態の場合のエラーレスポンス。
     * - 既に承諾済みの招待
     * - 既に期限切れの招待
     * - 既に取消済みの招待
     */
    409: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type RevokeOrganizationInvitationError = RevokeOrganizationInvitationErrors[keyof RevokeOrganizationInvitationErrors];

export type RevokeOrganizationInvitationResponses = {
    /**
     * 招待の取消に成功した場合のレスポンス（ボディなし）。
     */
    204: void;
};

export type RevokeOrganizationInvitationResponse = RevokeOrganizationInvitationResponses[keyof RevokeOrganizationInvitationResponses];

export type CreateAdminPortalSessionData = {
    body: AdminPortalSessionCreateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/admin/organizations/{organization_id}/admin-portal-sessions';
};

export type CreateAdminPortalSessionErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type CreateAdminPortalSessionError = CreateAdminPortalSessionErrors[keyof CreateAdminPortalSessionErrors];

export type CreateAdminPortalSessionResponses = {
    /**
     * Admin Portal セッションの作成に成功した場合のレスポンス。
     */
    201: AdminPortalSessionCreatedResponse;
};

export type CreateAdminPortalSessionResponse = CreateAdminPortalSessionResponses[keyof CreateAdminPortalSessionResponses];

export type GetMyOrganizationData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}';
};

export type GetMyOrganizationErrors = {
    /**
     * パスパラメータ `organization_id` が UUID 形式でない場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * リクエストユーザーが対象組織のメンバーでない場合のエラーレスポンス。
     * 情報漏洩防止のため、組織そのものが存在しないケースもこのステータスで返却します。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type GetMyOrganizationError = GetMyOrganizationErrors[keyof GetMyOrganizationErrors];

export type GetMyOrganizationResponses = {
    /**
     * 組織情報の取得に成功した場合のレスポンス。
     */
    200: MyOrganization;
};

export type GetMyOrganizationResponse = GetMyOrganizationResponses[keyof GetMyOrganizationResponses];

export type UpdateMyOrganizationData = {
    body: OrganizationUpdateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}';
};

export type UpdateMyOrganizationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     * - パスパラメータ `organization_id` が UUID 形式でない
     * - `display_name` が空文字・空白のみ・255 文字超過・文字列以外の型
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 認可に失敗した場合のエラーレスポンス。
     * - リクエストユーザーが対象組織のメンバーでない（情報漏洩防止のため組織不存在も同ステータス）
     * - リクエストユーザーのロールが Owner / Admin / Security Admin のいずれでもない（Member）
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type UpdateMyOrganizationError = UpdateMyOrganizationErrors[keyof UpdateMyOrganizationErrors];

export type UpdateMyOrganizationResponses = {
    /**
     * 組織情報の更新に成功した場合のレスポンス。
     */
    200: Organization;
};

export type UpdateMyOrganizationResponse = UpdateMyOrganizationResponses[keyof UpdateMyOrganizationResponses];

export type SendInvitationData = {
    body: OrganizationInvitationCreateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}/invitations';
};

export type SendInvitationErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 権限が不足している場合のエラーレスポンス。
     */
    403: ProblemDetails;
    /**
     * 指定された組織が外部プロバイダ（WorkOS）に存在しない場合のエラーレスポンス。
     * `type` は `https://auth.studio.design/problems/organization/external-organization-not-found`
     * (admin-scope の `.../problems/admin/external-organization-not-found` とは別 URI)。
     */
    404: ProblemDetails;
    /**
     * 競合が発生した場合のエラーレスポンス。
     * - 既に同じメールアドレスへの有効な招待が存在する場合
     * - 既に組織のメンバーである場合
     */
    409: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type SendInvitationError = SendInvitationErrors[keyof SendInvitationErrors];

export type SendInvitationResponses = {
    /**
     * 招待の作成に成功した場合のレスポンス。
     */
    201: OrganizationInvitationCreatedResponse;
};

export type SendInvitationResponse = SendInvitationResponses[keyof SendInvitationResponses];

export type ListMembersData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: {
        /**
         * 取得するページ番号（1始まり）。
         */
        page?: number;
        /**
         * 1ページあたりの取得件数。
         */
        per_page?: number;
        /**
         * ソート基準フィールド。
         * - `name`: ユーザー表示名
         * - `email`: ユーザーメールアドレス
         * - `role`: 組織内ロール
         * - `joined_at`: 組織への参加日時
         * - `last_access`: 組織への最終アクセス日時
         */
        sort_by?: 'name' | 'email' | 'role' | 'joined_at' | 'last_access';
        /**
         * ソート順。
         * - `asc`: 昇順
         * - `desc`: 降順
         */
        sort_order?: 'asc' | 'desc';
        /**
         * ロールでメンバーを絞り込みます。カンマ区切りで複数値を指定可能（例: `?role=owner,admin`）。
         * 未指定時は全ロールを返却します。
         */
        role?: Array<OrganizationRole>;
        /**
         * メンバーのメールドメインが組織の verified ドメインと一致するか (`corporate`)、しないか (`personal`) で絞り込みます。
         * カンマ区切りで複数値を指定可能（例: `?domain_type=corporate,personal`）。未指定時は全件返却します。
         */
        domain_type?: Array<OrganizationMemberDomainType>;
        /**
         * メンバーの表示名またはメールアドレスに対する部分一致検索文字列。
         * 1〜255 文字。空文字列はバリデーションエラー（400）となります。
         */
        search?: string;
        /**
         * `true` の場合、レスポンスのトップレベルに `role_counts` と `domain_counts`（フィルタ適用前の総件数集計）を含めて返却します。
         * 集計コストを避けるためデフォルトは `false`。
         */
        include_counts?: boolean;
    };
    url: '/organizations/{organization_id}/members';
};

export type ListMembersErrors = {
    /**
     * クエリパラメータが不正な場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * リクエストユーザーが対象組織のメンバーでない場合のエラーレスポンス。
     * 情報漏洩防止のため、組織そのものが存在しないケースもこのステータスで返却します。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type ListMembersError = ListMembersErrors[keyof ListMembersErrors];

export type ListMembersResponses = {
    /**
     * 組織メンバー一覧の取得に成功した場合のレスポンス。
     */
    200: OrganizationMemberListResponse;
};

export type ListMembersResponse = ListMembersResponses[keyof ListMembersResponses];

export type GetMemberMeData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}/members/me';
};

export type GetMemberMeErrors = {
    /**
     * パスパラメータ `organization_id` が UUID 形式でない場合のエラーレスポンス。
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * リクエストユーザーが対象組織のメンバーでない場合のエラーレスポンス。
     * 情報漏洩防止のため、組織そのものが存在しないケースもこのステータスで返却します。
     */
    403: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type GetMemberMeError = GetMemberMeErrors[keyof GetMemberMeErrors];

export type GetMemberMeResponses = {
    /**
     * 認証済みユーザーの組織メンバー情報の取得に成功した場合のレスポンス。
     */
    200: OrganizationMember;
};

export type GetMemberMeResponse = GetMemberMeResponses[keyof GetMemberMeResponses];

export type RemoveMemberData = {
    body?: never;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
        /**
         * メンバー識別子（UUID形式）。
         */
        member_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}/members/{member_id}';
};

export type RemoveMemberErrors = {
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     * - パスパラメータ `organization_id` または `member_id` が UUID 形式でない
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 認可に失敗した場合のエラーレスポンス。
     * - リクエストユーザーが対象組織のメンバーでない（情報漏洩防止のため組織不存在も同ステータス）
     * - リクエストユーザーのロールが Owner / Admin のいずれでもない
     */
    403: ProblemDetails;
    /**
     * 指定されたメンバーが組織内に存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * 競合が発生した場合のエラーレスポンス。
     * - リクエストユーザーが自分自身を削除しようとした場合（`cannot-delete-self`）
     * - 組織内の最後の Owner を削除しようとした場合（`last-owner-cannot-be-removed`）
     */
    409: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type RemoveMemberError = RemoveMemberErrors[keyof RemoveMemberErrors];

export type RemoveMemberResponses = {
    /**
     * メンバーの削除に成功した場合のレスポンス（ボディなし）。
     */
    204: void;
};

export type RemoveMemberResponse = RemoveMemberResponses[keyof RemoveMemberResponses];

export type UpdateMemberRoleData = {
    body: OrganizationMemberUpdateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
        /**
         * メンバー識別子（UUID形式）。
         */
        member_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}/members/{member_id}';
};

export type UpdateMemberRoleErrors = {
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     * - パスパラメータ `organization_id` または `member_id` が UUID 形式でない
     * - `role` が列挙値のいずれでもない
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 認可に失敗した場合のエラーレスポンス。
     * - リクエストユーザーが対象組織のメンバーでない（情報漏洩防止のため組織不存在も同ステータス、`membership-required`）
     * - リクエストユーザーが Owner でない状態で他メンバーのロールを変更しようとした（`owner-required`）
     */
    403: ProblemDetails;
    /**
     * 指定されたメンバーが組織内に存在しない場合のエラーレスポンス。
     */
    404: ProblemDetails;
    /**
     * 競合が発生した場合のエラーレスポンス。
     * - 組織内の最後の Owner のロールを変更しようとした場合（`last-owner-role-cannot-be-changed`）
     */
    409: ProblemDetails;
    /**
     * 自分自身のロール変更が降格でない場合のエラーレスポンス（`self-role-change-must-be-downgrade`）。
     * - 昇格（例: Member → Admin、Admin → Owner）
     * - 横移動（Admin ↔ Security Admin）
     *
     * レスポンス本体に `current_role` と `requested_role` が含まれます。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type UpdateMemberRoleError = UpdateMemberRoleErrors[keyof UpdateMemberRoleErrors];

export type UpdateMemberRoleResponses = {
    /**
     * メンバーロールの変更に成功した場合のレスポンス。
     */
    200: OrganizationMember;
};

export type UpdateMemberRoleResponse = UpdateMemberRoleResponses[keyof UpdateMemberRoleResponses];

export type CreateMyAdminPortalSessionData = {
    body: AdminPortalSessionCreateRequest;
    path: {
        /**
         * 組織識別子（UUID形式）。
         */
        organization_id: string;
    };
    query?: never;
    url: '/organizations/{organization_id}/admin-portal-sessions';
};

export type CreateMyAdminPortalSessionErrors = {
    /**
     * リクエストが不正な場合のエラーレスポンス。
     * - パスパラメータ `organization_id` が UUID 形式でない
     * - `intent` が欠落、空、不正値、非文字列型
     */
    400: ProblemDetails;
    /**
     * 認証に失敗した場合のエラーレスポンス。
     */
    401: ProblemDetails;
    /**
     * 認可に失敗した場合のエラーレスポンス。
     * - リクエストユーザーが対象組織のメンバーでない（情報漏洩防止のため組織不存在も同ステータス）
     * - リクエストユーザーのロールが Owner / Security Admin のいずれでもない（Admin / Member）
     */
    403: ProblemDetails;
    /**
     * 外部プロバイダ（WorkOS）に対応する組織が存在しない場合のエラーレスポンス。
     * DB 上はメンバー登録済みだが、プロバイダ側で組織レコードが削除されている等の
     * データ不整合で発生する。
     * `type` は `https://auth.studio.design/problems/organization/external-organization-not-found`
     * (admin-scope の `.../problems/admin/external-organization-not-found` とは別 URI)。
     */
    404: ProblemDetails;
    /**
     * バリデーションエラーが発生した場合のエラーレスポンス。
     */
    422: ProblemDetails;
    /**
     * レート制限超過。次のリクエストまで待機してください。
     */
    429: ProblemDetails;
    /**
     * エラーが発生した場合の Problem Details (RFC 9457) レスポンス。
     */
    500: ProblemDetails;
};

export type CreateMyAdminPortalSessionError = CreateMyAdminPortalSessionErrors[keyof CreateMyAdminPortalSessionErrors];

export type CreateMyAdminPortalSessionResponses = {
    /**
     * Admin Portal セッションの作成に成功した場合のレスポンス。
     */
    201: AdminPortalSessionCreatedResponse;
};

export type CreateMyAdminPortalSessionResponse = CreateMyAdminPortalSessionResponses[keyof CreateMyAdminPortalSessionResponses];

export type CheckSsoEnforcementData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * 判定対象のメールアドレス
         */
        email?: string;
        /**
         * 判定対象のユーザーID（UUID）
         */
        sub?: string;
    };
    url: '/api/sso-enforcement/check';
};

export type CheckSsoEnforcementErrors = {
    /**
     * リクエストパラメータが不正
     */
    400: ProblemDetails;
    /**
     * クライアント認証失敗
     */
    401: SsoEnforcementErrorResponse;
    /**
     * サーバー内部エラー
     */
    500: ProblemDetails;
};

export type CheckSsoEnforcementError = CheckSsoEnforcementErrors[keyof CheckSsoEnforcementErrors];

export type CheckSsoEnforcementResponses = {
    /**
     * SSO 強制判定結果
     */
    200: CheckSsoEnforcementResponse;
};

export type CheckSsoEnforcementResponse2 = CheckSsoEnforcementResponses[keyof CheckSsoEnforcementResponses];
